# pictkura - 開発ロードマップ（対話型AI向けプロンプト指示書）

このドキュメントは、Tauri + Rust を用いた画像管理アプリの開発をAIに段階的に依頼するためのステップバイステップの設計プランです。AIのコンテキスト迷走を防ぐため、各フェーズごとに独立して実装を依頼してください。

## プロダクト目標
- **マルチOS対応**（Windows / macOS / Linux）
- **USBから任意のコピー先を指定して取り込み**
- **爆速表示（本当に）** — 機能は最小限でよいが、速さだけは一切妥協しない

## 共通コンテキスト（セッション開始時に毎回AIに与える前提条件）
- **技術スタック**: Rust, Tauri v2, SQLite (`rusqlite`), フロントエンド(React または Vue)
- **アーキテクチャ**: バックエンド(Rust)が重い処理（ファイルI/O、サムネイル生成、DB管理）を担い、フロントエンドは表示のみを行う。
- **重要要件（爆速の原則）**:
  - ハッシュ(CRC等)計算によるファイル更新検知は禁止（サイズとmtimeの比較のみ）。
  - Base64エンコードは禁止。Tauri Custom Protocolを利用した画像バイナリのダイレクトストリーミングを行う。
  - フロントエンドはDOM爆発を防ぐため仮想スクロールを前提とする。
  - SQLiteは**WALモード**で開き、INSERTは**トランザクションでバッチ実行**する。
  - 画像の**幅・高さをDBに保存**し、グリッドが描画前に枠を確保できるようにする（レイアウトシフト＝体感の遅さを排除）。
  - サムネイルはまず**EXIF埋め込みサムネイルを即時利用**し、自前生成はバックグラウンドで後追いする。
  - サムネイル生成は**可視領域優先**（フロントが可視範囲のIDを通知し、ワーカーが優先処理）。

## UIデザイン方針（PlayMemories Home 風）
Sony PlayMemories Home のメイン画面構成を踏襲する:
- **3エリア構成**: 上部に「表示切替（サムネイル／カレンダー）＋検索」、左ペインに「すべての画像／お気に入り／カメラとメディア／フォルダ（ハードディスク）」のナビゲーション、中央に画像グリッド。
- **日付グルーピング**: 撮影日時で自動分類し、日付見出し付きのセクションとしてグリッド表示する。
- **サムネイルサイズスライダー**: 上部に配置し、グリッドの列数・サイズをリアルタイムに変更できる。
- **サムネイル上のオーバーレイアイコン**（RAW／動画などの種別表示）は将来対応（第2部）。
- MVPで作るのは「左ペイン＋日付グルーピングされたサムネイルグリッド＋サイズスライダー」まで。カレンダービュー（年／月／日切替）は第2部。

---
## 【第1部：コアMVP構築】ここまでで「最小機能だけど爆速」が完成する

### フェーズ1: コア構造とConfig（設定）の定義
**【AIへの指示・目標】**
UIを作らず、純粋なRustライブラリとして開始。
1. `[import]`, `[routing]`, `[library]`, `[performance]` の4つのドメインを持つ `Config` 構造体を定義。
2. TOMLファイルとのシリアライズ/デシリアライズ(`serde`)を実装。

### フェーズ2: 爆速ディレクトリスキャナーとDBスキーマの実装
**【AIへの指示・目標】**
1. SQLiteの `media` テーブル作成（パス、サイズ、mtime、**幅・高さ**、**撮影日時（EXIF DateTimeOriginal、なければmtime）** のカラム）。DBは**WALモード**で開く。
2. Configの「除外パターン」でディレクトリを枝刈り（Pruning）し、スキャンをスキップするロジックを実装。
3. OSのメタデータ（サイズ、mtime）とDBを比較し、差分ファイルのみを抽出する関数を作成。INSERT/UPDATEはトランザクションでバッチ実行。

### フェーズ3: Tauri Custom Protocol の MVP実装
**【AIへの指示・目標】**
1. Tauri v2を統合し、カスタムプロトコル `media://` を実装。
2. リクエストされたIDからSQLiteを引き、ローカルのJPEGバイナリを直接Webviewへストリーミングする処理を作成。

### フェーズ4: フロントエンドの仮想スクロール実装（PlayMemories Home風グリッド）
**【AIへの指示・目標】**
1. `TanStack Virtual` 等を導入し、仮想スクロールによるUI基盤を構築。
2. CSS Gridを用いたレスポンシブな画像グリッドを作成し、`media://` プロトコル経由で画像を描画。DBに保存した幅・高さで**描画前に枠を確保**する。
3. **撮影日ごとの日付見出し付きセクション**としてグリッドを構成する。
4. 左ペイン（すべての画像／フォルダツリー）と、上部の**サムネイルサイズスライダー**を実装。

### フェーズ5: 爆速サムネイル（埋め込み優先＋バックグラウンド生成）
**【AIへの指示・目標】**
1. スキャン直後は**EXIF埋め込みサムネイル**を抽出してそのまま配信し、一覧を即表示する。
2. UIをブロックしないワーカースレッド（MPSCチャネル等）で、`image` クレートによる高品質サムネイルを生成し、ローカルDiskへ保存してDBを更新する非同期フローを構築。
3. フロントエンドから可視範囲のIDを受け取り、**可視領域を優先**するジョブキューを実装。

### フェーズ6: USB取り込み（手動選択・任意のコピー先指定）
**【AIへの指示・目標】**
OS固有のUSB検知APIは使わず、まずは手動選択で目標を満たす。
1. フォルダ選択ダイアログでUSBドライブ（またはDCIMフォルダ）を指定させる。
2. Configの `[routing]` に従い、**任意のコピー先**（例: 日付別フォルダ構成）へコピーする処理を実装。コピー後にサイズ比較で検証する。
3. 取り込んだファイルを自動でスキャン対象に加え、即座にグリッドへ反映する。

### フェーズ7: 最小限のテスト
**【AIへの指示・目標】**
1. Configのパース、ディレクトリスキャン、差分検知、ルーティング（コピー先決定）ロジックに対する Rust標準の `#[test]` を実装。
2. `tempfile` クレートを用いた仮想ファイルシステム＋インメモリSQLiteでの結合テストは、上記が固まってから薄く追加。

---
## 【第2部：あとから足すもの（MVPでは作らない）】

- **カレンダービュー**: PlayMemories Home同様の年／月／日切替表示。日付グルーピング（フェーズ4）のデータをそのまま流用できる。
- **USB挿入の自動検知**: ドライブ一覧の5秒ポーリング（マルチOS・実装済み）で挿抜は検知できる。OS固有API（Windowsの `WM_DEVICECHANGE` 等）による即時検知＋自動取り込みトリガーは未実装。
- **RAW対応**: `libraw-rs` を統合し、RAWの埋め込みプレビューJPEGを抽出。
- **詳細表示モード／サムネイル上のオーバーレイアイコン**（RAW・動画・GPS等の種別表示）。
- **お気に入り・検索**（ファイル名・評価等での絞り込み）→ 検索は**第4部で実装済み**。
- **E2Eテスト**: Tauri WebDriver / Playwright による「USBフォルダ取り込み→サムネイル表示」の通しテスト。
- **CI/CD**: `cargo clippy` / `fmt` / `test` の自動実行、Windows/macOS/Linux のバイナリビルド。
  → **Windows は実装済み（2026-08-13〜14）**。`ci.yml`（検査）と `release.yml`（MSI＋ZIP）。
  macOS/Linux のジョブは未着手（rav1d の事情は引き継ぎ節を見ること）。
- **テレメトリ**: `tracing` による構造化ログ（I/O所要時間・メモリ計測）、日次ログ出力。
- **非UTF-8パス対応**（ゲート1レビューのP2繰り延べ）: 現状パスをTEXTで保存しており、Unixの非UTF-8ファイル名は `to_string_lossy` で情報が落ちる。Linux本格対応時にBLOB保存へ移行する。

---
## 【第3部：スケーラビリティ計画（1000万枚〜10億枚への道）】

現状は〜10万枚で爆速。スケールを阻むのは**UIへのデータの渡し方**であって、SQLiteやスキャナーではない。段階的に改修する。

### 段階A（〜100万枚）: 全件転送の廃止 ✅ **実装済み（2026-08-12）**
1. ✅ **タイムライン索引IPC**: `list_media`（全件JSON）を廃止し、「日付→枚数＋代表サムネイル」の
   サマリ（`timeline_summary`）と日単位取得（`list_day`）の2段APIへ。
   実装は `day_key`（ローカル日付YYYYMMDD整数、書き込み時に維持）＋複合インデックス
   `(day_key DESC, COALESCE(taken_at_ms, mtime_ms) DESC, id DESC)` によるインデックスシークで、
   キーセットページング相当をより単純な形で実現。フロントは日付索引から総高さを見積もり、
   可視範囲の日だけを取得・保持（スパースタイムライン、日キャッシュ上限150日）。
2. ✅ **差分検知のSQL化**: `stored_meta()` の全件HashMap読みを廃止し、スキャン結果を一時テーブルへ
   バッチ投入してSQLの外部結合で差分抽出・反映（`Db::apply_scan`、メモリO(1)）。
3. ✅ **サムネイルの分散配置とWebP化**: `thumbs/{id % 256}/{id}.webp`（lossy WebP、q=82）。
   即席（EXIF埋め込み）は再エンコード回避のためJPEGのまま。旧配置ファイルは再生成時に削除。
4. ✅ **イベントの差分更新**: thumb-ready後の全件再取得をやめ、更新後レコードを載せた
   `media-updated` イベントをpushし、フロントは該当日だけを部分パッチ。
   撮影日確定による日移動は検知して骨組み（サマリ）だけをデバウンス再取得。

### 段階B（〜1000万枚）: スキャンとサムネイルのオンデマンド化 ✅ **実装済み（2026-08-12）**
1. ✅ **変更ジャーナル**: 起動時にNTFSのUSNジャーナルで「前回終了以降の変更」だけを取得。
   レコードの親FRNを現在のディレクトリパスへ解決した「ダーティディレクトリ集合」を
   2の枝刈りウォーカーで再走査し、`apply_partial_scan` で影響範囲だけを反映する。
   非管理者でも動く（ルートディレクトリハンドル＋FSCTL_READ_UNPRIVILEGED_USN_JOURNAL）。
   初回・ラップ・非NTFS・変更多数（>512フォルダ）は枝刈りスキャンへフォールバック。
   ボリュームごとの読み取り位置はmetaテーブルに永続化（`usn|C:` キー）。
   macOS: FSEvents履歴 / Linux: fanotify は未対応（常にフォールバック）。
2. ✅ **ディレクトリmtime枝刈り**: dirsテーブル（ディレクトリ→mtime）と
   media.parent_dir列を追加。mtime一致のディレクトリはファイル列挙をスキップし、
   既知の子ディレクトリへの再帰だけ行う。削除判定は「実際に列挙したディレクトリ直下」
   に限定して誤削除を防止。拡張子・除外パターンのフィンガープリントが変わったら
   自動フルスキャン。手動「再スキャン」は常にフル（内容だけの上書きの修復手段）。
3. ✅ **サムネイルのオンデマンド生成＋LRU**: 自動処理は「メタデータ抽出＋即席」まで。
   高品質は可視領域要求（set_visible_priority）時のみ生成。thumb_bytes/thumb_used_ms
   列で利用を追跡（配信タッチはメモリ集約→ジャニターが60秒ごとにフラッシュ）し、
   上限容量（performance.thumb_cache_mb、既定4GB、0=無制限）超過分を古い順にLRU削除。
4. ✅ **読み取り接続プール**: media://配信とタイムライン系IPCをリードオンリー接続
   （2〜4本、ラウンドロビン＋try_lock空き探索）へ分散。
5. ✅ **⚡爆速メーター**（おまけ）: 起動同期の方式（USN差分/枝刈り/フル）・所要時間・
   差分件数をトースト表示。USNで変更ゼロの起動はファイルシステムを一切歩かない。

### 段階C（10億枚）: 設計だけ先に対応
10億枚＝数十PBはデスクトップのファイルシステムの領域を超える（NAS/オブジェクトストレージ前提）。
アプリ側は段階A/Bの「スパースタイムライン＋キーセットページング＋オンデマンドサムネイル」が
そのまま通用する設計なので、**ストレージ抽象（ローカルパス以外のバックエンド）** を差し込める
インターフェースにしておくこと以上は先回りしない。

**検証方法**: 合成データ生成ツール（ダミーレコードをDBへ直接1000万件投入）を作り、
実画像なしでUI・クエリの応答時間を計測するベンチを用意する。
→ ✅ `cargo run --release --bin bench -- --count <N>` として実装済み（2026-08-12）。

**ベンチ結果（2026-08-12, Windows 11, リリースビルド）**:

| クエリ | 100万件 | 1000万件 |
|---|---|---|
| timeline_summary（起動時の索引） | 127ms | 903ms |
| list_day（最多日1日分の取得） | 2.6ms | 33ms |
| list_memories（〇年前の今日） | 0.3ms | 0.4ms |
| timeline_summary(★のみ) | 28.3ms | 68.9ms |
| apply_scan（差分ゼロ再スキャン、メモリO(1)） | 12.1s | 未計測 |
| [旧] list_all 全件転送 | 1.7s＋JSON/IPC転送 | 実行不能級 |

DBサイズ: 100万件=177MB、1000万件=1.8GB。
サマリはO(全日数)のインデックススキャンのため件数に対し準線形。1000万件でも
起動時1回・秒未満で、以降のスクロールは可視日のシークのみ（数十ms）。

---
## 【第4部：探す・見つける（爆速検索）】✅ **実装済み（2026-08-12）**

第2部の宿題だった「検索」を、スケール（第3部）を壊さない形で実装した。
**検索結果もタイムラインのまま**（サマリ＋日単位取得の2段API）なので、
スパースタイムライン・カレンダー・ビューア・スクラバーがそのまま動く。

### 段階D-1: 撮影メタデータ
- ✅ **カメラ名のみDBへ**: `cameras` 表（名前の正規化）＋ `media.camera_id`。
  1000万行に数十バイトの文字列を持たせず、ファセット集計は `camera_id` の
  インデックスだけで返る。`camera_id` の意味は NULL=未確認 / 0=確認済みだが
  情報なし / 正=`cameras.id`。
- ✅ **レンズ・絞り・シャッター・ISO・焦点距離・GPSは列に持たない**。
  表示するのは常に1枚だけなので、ビューアを開いた瞬間に実ファイルのEXIF
  ヘッダを読む（`get_exif_info`、数百µs）。DBは1バイトも増えず、常に実物と一致する。
- ✅ 既存ライブラリの後追い補完: メタデータ抽出済みでカメラ未確認の行を、
  起動後にバックグラウンドでEXIFヘッダだけ読んで埋める（画像デコードなし）。

### 段階D-2: 爆速検索（FTS5＋日本語N-gram）
1. ✅ **全文索引**: `media_fts`（FTS5, `content=''` + `contentless_delete=1`）に
   ファイル名と親フォルダ（末尾3階層）を索引。本文は複製せず索引だけを持つ。
   同期はmediaへの**トリガ**で閉じており、スキャン経路が増えても書き忘れない。
2. ✅ **日本語の中間一致**: unicode61はCJKの連続を1トークンにするため
   「沖縄旅行.jpg」から「旅行」が引けず、trigramは2文字クエリ（日本語で最頻）が
   効かない。そこで**索引を書くときにCJKの連続だけをbigramへ展開**する
   （沖縄旅行→「沖縄 縄旅 旅行」）。クエリ側も同じ展開をして**フレーズ検索**に
   するので、位置が連続するbigramだけが当たり誤検出も出ない。1文字は前方一致。
3. ✅ **カメラはFTSに載せない**: 機種数は数十しかない低カーディナリティの
   ファセット。`cameras` 表を引いて `camera_id IN (...)` のインデックスシークに
   落とす。100万件で `camera:` が **233ms → 6.2ms**、索引も42MB小さくなった。
   自由語は「FTS一致 **OR** カメラ名一致」を語ごとに組むので、
   検索ボックスに `iPhone` と打っても拾える。
4. ✅ **prefix索引は張らない**: 打鍵途中の前方一致は用語辞書のレンジスキャンで
   足り、100万件の実測で `prefix='2 3'` ありと速度差がなかった（1文字「沖」でも
   ヒット件数が支配的）。一方でprefix索引は容量+48%・構築時間2倍のコスト。
5. ✅ **クエリ言語**: `沖縄`（自由語）／`camera:α7`／`folder:旅行`／
   `2019年` `2019-08` `2019年8月11日`（撮影日の範囲）／`★`（お気に入り）。
   条件はすべてAND。裸の4桁数字は日付にしない（ファイル名の検索語と区別できないため）。
6. ✅ **既存ライブラリの索引化**: 起動を1msも遅らせないため、範囲を固定して
   バックグラウンドで少しずつ掃き寄せる（構築中に増えた行はトリガが直接索引化するので
   二重投入にならない）。進捗はトーストで表示。索引の構成を変えたら
   `fts_schema` の版で作り直す。

### 段階D-3: UI
- ✅ 上部バーの**検索ボックス**（160msデバウンスのインクリメンタル検索）
- ✅ **コマンドパレット（Ctrl+K）**: 日付ジャンプ・カメラ絞り込み・全文検索・
   アプリ操作を1か所から。候補はすべてメモリ上の骨組みから作るので打鍵ごとのIPCなし
- ✅ 左ペイン **「カメラとメディア」**（PlayMemories Home風のファセット）
- ✅ **タイムライン・スクラバー**: 右端の年目盛り。10年分でも1クリックで飛べる
- ✅ ビューアの**撮影情報パネル**（`I` キー / ℹボタン）。GPSは地図リンク付き

**ベンチ結果（2026-08-12, 100万件, リリースビルド）**:

| 検索 | ヒット | count | summary | 1日取得 |
|---|---|---|---|---|
| `沖` （1文字・前方一致） | 125,569 | 170ms | 186ms | 17ms |
| `沖縄` （日本語2文字） | 125,569 | 137ms | 188ms | 17ms |
| `旅行` （**中間一致**） | 125,569 | 138ms | 181ms | 14ms |
| `IMG_00000` （打鍵途中） | 10,000 | 3.4ms | 5.4ms | 1.8ms |
| `IMG_000001` | 1,000 | 0.4ms | 0.7ms | 0.3ms |
| `camera:α7` （ファセット） | 199,353 | 6.2ms | 216ms | 2.2ms |

索引の構築 4.5秒／DBサイズ +120MB（247MB→366MB, 1件あたり約120バイト）。
広くヒットする語ほどsummaryのGROUP BYが支配的になる（＝絞り込めていない検索ほど遅い）
が、実用的な検索は数千件以下に落ちるためミリ秒で返る。

### 未着手（第4部の残り）
- 検索結果の**並び替え**（関連度順など）。現状は常に撮影日時の新しい順。
- 索引の**再構築UI**（壊れた場合の手動リセット）。今は `fts_schema` の版でのみ作り直す。
- **RAW・動画**のメタデータ（第2部から継続）。

---
## 【第5部：取り込みウィザード（USB/SDカードを見てから取り込む）】✅ **実装済み（2026-08-12）**

第1部フェーズ6の取り込みは「フォルダを選ぶ→丸ごとコピー」だった。
中身が見えないので、**どれが新しい写真なのか分からないまま**取り込むしかない。
PlayMemories Home のように「挿したメディアの中を見て、必要な分だけ入れる」を作る。

### 段階E-1: 取り込み元のツリー閲覧（`browse.rs`）
- ✅ **1階層ずつしか読まない**。ツリーを開いた分だけ `read_dir` する。
  USBを再帰で舐めると数万ファイルのカードで数十秒固まるため、
  スキャナ（[`scanner`]）は取り込み本体にだけ使う。
- ✅ 子フォルダのバッジ（直下の画像枚数・下の階層の有無）は
  `read_dir` が返す `file_type` だけで判定する（1件ずつの `stat` をしない）。
  数え上げは**2000件で打ち切って「N+」**と出す（開くのを待たせない）。
- ✅ 除外はゴミ箱・システム領域だけ。ライブラリ用の `exclude_patterns` は
  **使わない**（ユーザーの除外設定がDCIMに誤マッチして静かに取りこぼすため）。

### 段階E-2: 取り込み前のサムネイル（`media://src/<16進パス>`）
- ✅ DBに無いファイルを配信するので**パスで指す**。Windowsのパスは `\` `:` を含み
  WebViewごとにURLの正規化が違うため、**UTF-8バイト列の16進**で運ぶ
  （URLに出るのは `[0-9a-f]` だけ）。Base64禁止の原則どおり、
  画像はIPCではなくプロトコル経由でストリーミングする。
- ✅ **EXIF埋め込みサムネイルを再エンコードせず返す**（ヘッダ数十KBの読み出しだけ。
  フルデコードは埋め込みの無いPNG等に限る）。USBの遅さがそのまま体感になるため、
  ここは第1部フェーズ5と同じ「デコード回避」を徹底する。
- ✅ 配信先は**UIが実際に開いたフォルダの直下だけ**に限る（`browse_allow`）。
  ライブラリ外の任意パスを読めるURLになるため、細工したURLで
  `C:\Windows\...` を読み出せないようにする。
- ✅ `media://` のハンドラを**非同期登録に変更**した。同期ハンドラはメインスレッドで
  走るため、USB読み＋デコードが混ざるとUI全体が固まる。

### 段階E-3: 「取り込み済み」の後追い判定
- ✅ 一覧表示は待たせず、**表示後に100件ずつ**「コピー先に同じファイルがあるか」を
  判定して「済」バッジを塗る。判定できた分から**未取り込みだけを自動選択**するので、
  開いて取り込みボタンを押すだけで新しい写真だけが入る。
- ✅ 判定は取り込み本体と**同じ経路**（同じ日付決定＋同じ衝突回避）を通す
  （`is_already_imported`）。「済と出たのにまたコピーされた」を構造的に防ぐ。

### 段階E-4: UI
- ✅ 左＝ドライブとフォルダのツリー（リムーバブルは🔌、DCIMは自動で開く）
- ✅ 右＝サムネイルのグリッド（チェックで選択、済は淡く表示、300件ずつ描画）
- ✅ 下＝コピー先・振り分け（設定と同じプリセット）・「フォルダごと」「N枚を取り込む」
- ✅ 選んだ分だけ取り込む `import_paths`（走査せず、渡されたパスだけをコピー）。
  取り込み後の後始末（コピー先をライブラリへ追加＋そのルートだけ再走査）は
  フォルダ丸ごと取り込みと共通の経路を通る。

**実測（2026-08-12, Canon EOS の SDカード G:\DCIM@CANON = JPEG 174枚, リリースビルド）**:

| 操作 | 時間 |
|---|---|
| `list_dir`（174ファイルの一覧＋枚数バッジ） | 0.3ms |
| `preview` 30枚（画面1枚分のサムネイル） | 4.2ms（1枚 0.14ms・平均17KB） |

計測は `target
eleaseench.exe --browse <パス>`。埋め込みサムネイルをそのまま
返すため、1枚あたりのコストはEXIFヘッダの読み出しだけになる（デコード無し）。
※OSのファイルキャッシュが温まった状態の値。挿した直後の初回は
カードの読み出し速度に律速される。

### 段階E-5: 「どこに入っているか分からない」人向けの自動走査
- ✅ **フォルダを選んだら既定で下の階層まで走査**する（`list_tree`）。
  リムーバブルを1回クリックすればカードの中身が全部並ぶので、
  DCIM→100CANON…とツリーを辿る知識が要らない。上限2万件で打ち切り、
  超えたら「フォルダごと取り込む」へ誘導する。
- ✅ 例外は**固定ドライブとネットワークドライブの直下**。`C:\` や `Z:\` 全体を
  舐めない。ドライブ種別は `GetDriveTypeW`（`DRIVE_REMOVABLE` /
  `DRIVE_FIXED` / `DRIVE_REMOTE` / `DRIVE_CDROM`）で判定する。
  `sysinfo` は removable かどうかしか教えてくれないため直接呼ぶ。
  なお**USB接続のHDD/SSDは fixed と申告される**ことが多いので、
  「リムーバブルだけ特別扱い」ではなく「固定ドライブの**直下だけ**除外」にした。
- ✅ **クラウドのプレースホルダはサムネイルを取りに行かない**。OneDriveや
  iCloud Driveは「ドライブ」ではなくCドライブ上のフォルダなので種別では
  見分けられないが、`FILE_ATTRIBUTE_OFFLINE` / `RECALL_ON_OPEN` /
  `RECALL_ON_DATA_ACCESS` が立っている。読むだけで実体のダウンロードが
  走るため、一覧では☁マークだけを出す（取り込めば普通に落ちてくる）。
- ✅ **取り込み済みを隠す**（既定ON）。見たいのは「まだ入っていない写真」。
  一括選択も**見えているものだけ**を対象にする（隠れた済が紛れ込まない）。
- ✅ **コピー中の待ち時間**: 進捗バー＋残り時間の見積もり＋
  **いま入れている写真のサムネイル**（進捗イベントにパスを載せる）。

### 段階E-6: 取り込み中のDBロック（実測で見つけた不具合）
400件を一度に取り込むと「DB操作に失敗: database is locked」で終わっていた。
コピー先の**ファイル監視のupsert**と**取り込み後の反映スキャン**が別接続で
同時に書きに行くため。書き込みトランザクションが既定の `DEFERRED` だと、
最初のSELECTで読み取りスナップショットを取ってから書き込みロックへ昇格する間に
他の接続がコミットしていて `SQLITE_BUSY_SNAPSHOT` が**`busy_timeout` を無視して
即座に**返る（第4部で `fts_build_step` だけ個別に対処していた罠が、
取り込みの規模が上がって他の経路でも出た）。
→ ✅ `Db::write_tx()` を作り、**すべての書き込みトランザクションをIMMEDIATE**に統一。
再現テスト（3スレッドが `apply_scan` と `upsert_files` を交互に叩く）付き。

### 未着手（第5部の残り）
- **RAW・動画**（プレビューは埋め込みJPEGを読めるが、拡張子が対象外のまま）
- ツリーのキーボード操作、取り込み中の中断

---
## 【第6部：RAW対応】✅ **実装済み（2026-08-12）**

### 段階F: 現像せず「カメラが書いた絵」を使う
RAWの現像（デモザイク＋色変換）は1枚あたり数百ms〜秒かかり、「爆速」と両立しない。
一方、実用上のRAWは**例外なくカメラが書いた表示用JPEGを内部に持っている**
（撮影時に背面液晶へ出すため）。これを取り出せば、一覧もビューアもデコード1回で済む。

- ✅ **抽出は3段構え**（`raw.rs`）:
  1. **TIFFの申告**（CR2・NEF・ARW・DNG・ORF・PEF・RW2 等）。複数のIFDから
     `JPEGInterchangeFormat` と、旧JPEG圧縮(`Compression=6`)の `StripOffsets` を集め、
     **一番大きいJPEG**を選ぶ。ヘッダだけで位置が分かるので全体を読まない
  2. **ブロック走査**（CR3・RAF 等、TIFFではない形式）。先頭16MBから
     SOI〜EOIの塊を拾い、最大のものを採る。形式ごとのパーサを書かずに届く
  3. 見つからなければ諦める（一覧には枠だけ出る）
- ✅ **CR3のメタデータ**: CR3はISO-BMFFなのでEXIFパーサが直接読めない。
  Canonは `moov/uuid` の下の `CMT1`（IFD0）・`CMT2`（Exif IFD）に**TIFFのまま**
  入れているので、箱を辿って中身をEXIFパーサへ渡す（読むのはヘッダ周辺だけ）。
  さらに、箱の中身は「IFD0にExifのタグ番号が並んでいる」ように見える
  （EXIFのタグは(文脈, 番号)の組なので文脈違いは別タグ扱い）ため、
  **文脈を跨いでタグを引く**（`field_any_context`）。分数のまま出る値には単位を足す
- ✅ サムネイル生成はRAWだけ経路を変える: 埋め込みプレビューは数MBあるので
  「即席サムネイルとしてそのまま保存」はせず、**最初から縮小して高品質版を作る**
- ✅ ビューアの原寸表示（`media://full`）もRAWは埋め込みプレビューJPEGを返す
  （`.CR3` をそのまま返してもWebViewは描けない）
- ✅ 既定の対象拡張子にRAWを追加。既存の設定は**旧既定と完全一致のときだけ**
  引き上げる（自分で編集した設定は尊重する）

**実測（Canon EOS R8 / CR3 30.8MB, リリースビルド）**:

| 処理 | 時間 |
|---|---|
| 埋め込みプレビュー抽出（6000x4000・1.7MB） | 17ms |
| EXIF（機種・撮影日時・レンズ・絞り・ISO・焦点距離） | 22ms |

計測は `target
eleaseench.exe --raw <パス>`。

### 段階F-2: 実ファイルでのカバレッジ確認（2026-08-12）

各社のCC0サンプル（raw.pixls.us）22ファイル・19拡張子で実測した。
`target
eleaseench.exe --raw-dir <フォルダ>` で表を作り直せる。

| 形式 | 機種 | 取り出せたプレビュー |
|---|---|---|
| CR2 | Canon EOS 20D / 40D | ✓ 1536x1024 / 1936x1288 |
| CR3 | Canon EOS R6 / R8 | ✓ 3408x2272 / 6000x4000 |
| NEF | Nikon D2H / COOLSCAN IV | ✓ 570x375 / 869x593 |
| NRW | Nikon COOLPIX P1000 | ✓ 160x120 |
| ARW | Sony α7S | ✓ 1616x1080 |
| RAF | Fujifilm FinePix S5000 | ✓ 1280x960 |
| ORF | Olympus E-10 | ✓ 160x120 |
| RW2 / RWL | Panasonic LX7 / Leica D-LUX 5 | ✓ 1920x1920 |
| PEF | Pentax K10D | ✓ 3872x2592 |
| SRW | Samsung NX500 | ✓ 6480x4320 |
| X3F | Sigma DP1 | ✓ 2640x1760 |
| KDC | Kodak P712 | ✓ 3072x2304 |
| DNG | **Apple iPhone 12 Pro（ProRAW）** | ✓ 4032x3024（フル解像度） |
| DNG | Leica M8 | ✓ 320x240（非圧縮RGB） |
| ERF | Epson R-D1 | ✓ 160x120（非圧縮RGB） |
| 3FR | Hasselblad H3D | ✓ 320x240（非圧縮RGB） |
| IIQ | Phase One P40+ | ✓ 304x220（非圧縮RGB） |
| DCR | Kodak DCS760C | ✓ 380x252（非圧縮RGB・16ビット） |
| DNG | Blackmagic Micro Cinema | ✗ プレビューを持たない（生データのみ） |
| MRW | Minolta DiMAGE 5 | ✗ サムネイルの先頭バイトを潰す独自仕様 |

**23ファイル中21ファイルで取得できる**（抽出は2〜48ms）。この過程で4つ直した:

1. **一番大きいJPEGが表示用とは限らない**。CR2はセンサーの生データを
   *ロスレスJPEG(SOF3)* として抱えており、大きさで選ぶと必ずそれを掴む。
   → SOFマーカーを見て「8ビット・成分1〜4のベースライン系」だけを採る
2. **EOIを探すだけでは切れる**。JPEGのEXIF(APP1)には**サムネイルJPEGが丸ごと**
   入っていることがあり、内側のEOIで切ると壊れた画像になる（RAFで発生）。
   → セグメントを長さで辿り、SOSの後ろは詰め物とリスタートを飛ばして終端を決める

3. **JPEGを1枚も持たない形式がある**（旧世代・中判: Leica DNG・Epson ERF・
   Hasselblad 3FR・Phase One IIQ・Kodak DCR）。IFDを調べると、プレビューを
   **非圧縮のRGB**としてTIFFのストリップに置いていた。
   → `Compression=1` かつ `Photometric=2`（RGB）のIFDからストリップを繋いで
   絵にする経路を追加（センサーの生データ `Photometric=32803` は選ばない）。
   16ビットのものは**リニア**（ガンマ未適用）で入っており、上位8ビットを
   そのまま使うと真っ暗になるため、sRGB相当のガンマを掛けてから8ビットへ落とす

4. **原寸表示で向きが抜けていた**。埋め込みプレビューはカメラが回転前の状態で書き、
   しかも**EXIFを持たないことが多い**（CanonのCR3・AppleのProRAW）。普通のJPEGなら
   ブラウザがEXIFを見て回してくれるが、向き情報の無いJPEGを渡すと縦位置の写真が
   横倒しで出る。→ `raw_display_jpeg` で**向きを適用してから**返す
   （Orientation=1 のときは再エンコードせずそのまま返す）

**Apple ProRAW（iPhone）について**: 実体はDNG。iPhone 12 Proの実ファイルで確認したところ、
IFD0に**センサーフル解像度（4032x3024）の表示用JPEG**が単一ストリップで入っており、
`Compression=7`（JPEG）の申告を読む経路がそのまま通った（抽出21ms）。
撮影情報もレンズ名まで取れる。なお iPhone 16 Pro 世代は本体がJPEG XL圧縮でも
**プレビューは従来どおりJPEGで別IFDに置かれる**（Exiv2の実ファイル報告より）ため、
JPEG XLをデコードできなくても表示はできる見込み。15 Pro/16 Proの実サンプルは
ライセンスが明記された配布が見つからず未検証。

**テストの持ち方**: 実物のRAWはリポジトリに置けない（1ファイル数十MB）ので、
- **合成TIFFのユニットテスト**を本命のゲートにする（LE/BE・複数ストリップ・
  16ビット・生データIFDを選ばないこと）。実ファイル不要でCIに乗る
- **実サンプルは環境変数でオプトイン**（`PICTKURA_RAW_SAMPLES=<フォルダ>`）。
  無ければskip、あれば「取れたものは必ずデコードできる」ことを検証する
- `bench --raw-dir` の表は、人が見る回帰確認として残す

残る2ファイルは**そもそもプレビューが無い**（Blackmagic CinemaDNGは生データのみ）か、
**サムネイルの先頭バイトを潰す独自仕様**（Minolta MRW）。どちらも一覧には出るが
サムネイルは付かない。

### 未着手（第6部の残り）
- **動画**（第2部から継続）

---
## 【第7部：HEIC/HEIF対応（iPhoneの既定形式）】✅ **実装済み（2026-08-12）**

### 段階G: 現像はしないが、デコードは避けられない

RAWは「カメラが書いた表示用JPEGを取り出す」で済んだが、HEICは事情が違う。
**実サンプル（iPhone 16 / iPhone 8）を解剖して確かめた中身**:

- 主画像は `grid` アイテム＝**HEVCタイルの寄せ集め**（5712x4284 が45枚のタイル）
- **JPEGは1枚も入っていない**。`Exif` アイテムはあるが **IFD1が無い**ので
  EXIFサムネイルも無い。つまり「取り出すだけ」の手は使えない
- 向きは EXIF Orientation と `irot` の**両方**に書かれていて、実測では一致していた

そこで**役割を分ける**（`heif.rs`）:

1. **メタデータは自前で読む**。`meta` ボックスだけを辿って `pitm` → `iprp/ipco` →
   `ipma` と引き、主画像の `ispe`（寸法）・`irot`（回転）・`imir`（鏡映）を取る。
   トップレベルの箱はヘッダ8バイトずつ飛ばすので**画素を1バイトも読まない**（実測0.2〜0.6ms）。
   撮影日時・カメラ名は kamadak-exif が HEIF コンテナを読めるのでそのまま使える
2. **画素はOSのデコーダに任せる**。Windowsは **WIC**（HEIF Image Extensions）。
   自前でHEVCを抱えると libde265/x265 系のC依存とライセンス（LGPL/GPL）を
   背負うことになり、MITで配る方針と合わない。macOSは将来 ImageIO
3. デコーダが無い環境では**一覧には出るがサムネイルが付かない**
   （RAWでプレビューを持たない機種と同じ扱い）

**罠**: 向きを二重に掛けない。WICは `irot` を**適用して返す**ので、EXIF Orientation を
重ねると横倒しになる。かといって「必ず適用済み」と決め打つと、そうでないデコーダで
倒れたまま出る。判定は**縦横比で行う**（`needs_manual_transform`）。
最初に「原寸の格納寸法と実寸が一致するか」で判定したら、**サムネイルだけ二重回転した**
（倍率が違うと寸法が一致しないため）。180度回転・鏡映のみ・正方形は寸法から判別できないので、
その場合は「デコーダが適用済み」に倒す。

**実測（iPhoneのHEIC 14枚）**: 14件すべて表示可。コンテナ読み0.2〜0.6ms、
**埋め込みサムネイル約22ms**、主画像の展開260〜480ms。
一覧の即席サムネイルは埋め込み（小さいHEVC）から作るので主画像展開の**1/13**で済む。

**テストの持ち方**: RAWと同じく**合成コンテナのユニットテストを本命**にする
（`ispe`/`irot`/`imir`/`ipma` を持つ最小のHEIFをその場で組み立てる。実ファイル不要）。
実サンプルは `bench --heic <file>` / `--heic-dir <dir>` で人が見る回帰確認。

### 段階G-2: クラウドにしか実体が無いファイル（実測で見つけた事故）

HEICを既定の対象にした瞬間に踏んだ。Windowsの「ピクチャ」は既定で OneDrive へ
リダイレクトされている環境が普通にあり、**実測した環境ではライブラリ内のHEIC 3322枚のうち
94%がクラウドのみ**だった。これは特殊な使い方ではなく、**iPhoneで撮った写真を
クラウド経由で同期していれば自然にこうなる**（＝HEICを持っている人の多数派）。サムネイルの自動パスはこれを片端から開くので、
アプリを入れただけで**数GBのダウンロードが静かに始まる**。

Fableに他社の挙動を調べさせた結果（要点）:
- **Microsoft自身**が「インデクサやウイルス対策は `RECALL_ON_DATA_ACCESS` の
  ファイルを読むな」と案内し、Windowsのサムネイルキャッシュも `OFFLINE` 属性を飛ばす。
  さらにユーザー操作起因でないハイドレーションは**OSがトースト通知で警告する**
- **digiKam** は仮想プレースホルダのフォルダをコレクションとして**非サポートと明記**
- **Lightroom Classic** は全量ダウンロードさせ、コミュニティが回避策を案内している状態
- **Windowsフォト** は本体を開かず同期プロバイダ経由でサムネイルだけ取る

**採った方針**: 自動（バックグラウンド）パスでは触らない。ユーザーがその場所を
実際に見たとき（可視要求）に初めて取りに行く（`cloud.rs`／`ThumbOutcome::Deferred`）。
設定項目は増やさない（「常にこのデバイスに保持する」はOneDrive側に既にある）。
並び順は `COALESCE(taken_at_ms, mtime_ms)` なので、EXIFを読まなくても一覧には並ぶ。

判定はディレクトリ項目の属性だけを見るので**この判定自体はダウンロードを誘発しない**。
Windowsは `OFFLINE`/`RECALL_ON_OPEN`/`RECALL_ON_DATA_ACCESS`、
macOSは `SF_DATALESS`。Linuxは共通の機構が無いが、主要クライアントが完全同期なので問題が起きない。

**実測での確認**: 起動してもクラウドのHEIC 3033枚は1枚も落ちず（289/3322のまま）、
検索で1枚だけ画面に出したら**その1枚だけ**落ちた（290/3322）。

### 段階G-3: パスの綴りを揃える（実測で見つけた重複）

段階G-2の検証中に、**同じ写真が2件登録される**のを見つけた。

```
2058  C:/Users/me/OneDrive/画像\カメラ ロール\2020\04\20200428_065714560_iOS.heic
5390  C:\Users\me\OneDrive\画像\カメラ ロール\2020\04\20200428_065714560_iOS.heic
```

経路によってパスの綴りが違う。通常スキャンは**設定のライブラリルート**から辿るので
ルートの綴り（`C:/...`）を引きずり、USNジャーナルの差分起動は
`GetFinalPathNameByHandleW` が返す**全部バックスラッシュ**の綴りで辿る。
DBはパスを文字列として比較する（`ON CONFLICT(path)`）ので別のファイルになる。

HEIC対応でクラウドのファイルを取り寄せた（＝USNに変更が載った）だけで32件出た。
**原理的にはファイルを編集しただけでも起きる既存の不具合**で、
しかも次のフルスキャンで「どのルートにも属さない」と判定されて消えるため、
重複が出たり消えたりする。

**対処**（`paths.rs`）: DBへ書くパスは必ず綴りを揃える
（Windowsは区切りをバックスラッシュへ統一＋ドライブ文字を大文字。
実体の解決はしない——ファイルを開かない方針を崩さないため）。
既存のDBは開いたときに一度だけ書き換え、重複は
**サムネイルが進んでいる方を残して**統合する（`path_schema` = v1）。

**罠**: `root_case_sql`（削除保護の判定）で使うルートの綴りも同時に揃えること。
片方だけ正規化すると「どのルートにも属さない」と判定され、**保護が外れて全部消える**。

### 段階G-4: よくある形式をひととおり（bmp / gif / tiff / svg / avif）

HEICで「一覧に出せない形式があると困る」ことがはっきりしたので、
残っていた普通の形式もまとめて入れた。**どれも新しいCライブラリを足していない**。

| 形式 | サムネイル | 原寸表示 | 備考 |
|---|---|---|---|
| bmp / gif | `image` クレート（feature追加のみ） | 原本をそのまま | GIFはビューアで動く |
| tiff | `image` クレート（feature追加のみ） | **JPEGへ詰め直す** | WebViewがTIFFを描けない |
| svg | **作らない**（原本を配る） | 原本をそのまま | 寸法は `viewBox` から読む |
| avif | OSのデコーダ（WIC） | 原本をそのまま | WebViewが直接描ける |

**SVGはサムネイルを作らない**のが要点。WebViewがベクタを描けるので、
ラスタライザ（resvg等。フォント一式が付いてくる）を抱えずに済み、
拡大しても劣化しないという利点もそのまま残る。アプリに要るのは寸法だけなので、
`<svg>` の開始タグから `width`/`height`（無ければ `viewBox`）を読む
（XMLパーサも持ち込まない）。

**AVIFはHEIFと同じISO-BMFF**なので、コンテナの読み取り（`ispe`・`irot`）は
そのまま使い回せる。画素はWICのAV1デコーダ頼りだが、**AV1 Video Extensionが
無い環境でも一覧は成立する**——寸法はコンテナから取れるので枠は正しく確保でき、
サムネイルが作れないファイルは原本を配ってWebViewに描かせる（実測で確認）。

原本をそのまま返して良いかの判定は `thumbs::needs_display_transcode` に集約した。
RAW・HEIC・TIFFだけが「詰め直しが要る」側で、残りはブラウザに任せる。

### 段階G-5: 「サムネイルを作らない」ことの代償を測る

一覧に原本をそのまま配る経路（SVG・デコーダの無いAVIF・生成前のファイル）が
増えたので、速度への影響を実測した。

| | 1タイルあたり |
|---|---|
| 通常のサムネイル（WebP 512px） | **49KB**（実測2148件の平均） |
| 実写AVIFの原本（4032x3024） | **1.11MB**＝約23倍、しかも**1200万画素の展開**が要る |

実写AVIFを40枚並べたところ、**半分のタイルが最後まで描かれなかった**
（ブラウザの画像デコード予算切れ）。原本フォールバックは無条件では成立しない。

**対処**: 原本を一覧へ配るのは**長辺2048px以下**のときだけにした
（`MAX_ORIGINAL_THUMB_EDGE`）。大きすぎるものは空の枠のままにする方が
速くて予測できる。ベクタ（SVG）は「原寸」が無く、ブラウザがタイルの大きさで
描くので上限を掛けない。

**罠**: この判定は**配信のたびに通る**ので、上限をユーザー設定から計算しようと
して設定のロックを取ってはいけない。起動直後のスキャンがそのロックを持っている
間、一覧が丸ごと止まる（実測で踏んで定数へ戻した）。

なお、AV1 Video Extension が入っている環境ではAVIFも普通にサムネイルが作られる
ので、この上限に当たるのは**デコーダが無い環境のAVIF**と
**サムネイル生成前の大きな画像**だけ。

### 段階G-6: AVIFのデコーダを同梱する（マルチOSの線引き）

段階G-5の時点でAVIFはOSのデコーダ（WIC）頼みだった。ところが実測すると、
**この開発機（Windows 11）でもAVIFは1枚も展開できなかった**。
`AV1 Video Extension` は無料だが**Windows 11でも既定では入っていない**
（HEVC Video Extensions は 22H2 から既定で入るのと対照的）。
つまり「一部の環境で穴」ではなく **Windows既定では常に穴**だった。

そこで**AVIFだけデコーダを同梱**する。使うのは `rav1d`（dav1d の純Rust移植）。

**なぜAVIFだけか（HEICは今まで通りOS任せ）**

| | AVIF (AV1) | HEIC (HEVC) |
|---|---|---|
| 特許 | AOMediaがロイヤリティフリー | プールが**デコーダの配布にも課金**（Via LA: 年10万本超で$0.30/本）。Firefoxすら同梱を避けOS/HWへ委譲 |
| 純Rust実装 | `rav1d`（BSD-2-Clause） | 唯一の実用候補が**AGPL-3.0**。そもそも特許は実装の出自と無関係に掛かるので、自作しても回避にならない |
| 判断 | **同梱して全OSで同じ絵を出す** | **OSに委託**（ライセンスごと）。Fedora（libheifをAV1のみでビルド）・GIMP・Firefoxと同じ線 |

**実測（4032x3024・この開発機）**

| | 結果 |
|---|---|
| ライセンス | BSD-2-Clause。義務は著作権表示の再掲だけで、依存ツリーには既に同ライセンスが居る |
| `x86_64-pc-windows-gnu` | 通る。ただし `asm` feature は**NASMをビルド時に要求する**（msvcでも同じ） |
| `aarch64-apple-darwin` | **crates.io版は通らない**。パッケージに `src/arm/asm-offsets.h` が入っていない（引き継ぎ節「macOSで作業するとき」） |
| デコード | **40〜53ms**（`asm` 有り）。**無しだと156〜190ms** で6倍遅いので有効にする |
| バイナリ増分 | **+2.79MB**（10bit対応込み。24.68→27.61MB） |
| HEVCへの流用 | **不可**。rav1dはAV1専用 |

HEICをWICで展開すると260〜480msなので、**同梱した方がOS任せより速い**という逆転が起きている。

**実装の要点**

- コンテナ（`heif.rs`）に `iloc` / `iinf` / `iref(dimg)` / `idat` / `av1C` / `clap` の
  読み取りを足し、主画像の材料を集める（`read_avif_source`）。**画素はまだ触らない**
- 画素（`av1.rs`）は rav1d のC API を薄く包む。Rust APIは未整備なので、
  `dav1d_open` / `send_data` / `get_picture` を直接呼ぶ
- **縮小をYUV→RGB変換に融合する**。一覧用は目標の2倍まで整数間引きで落とし
  （輝度は step 四方の平均。間引くだけだと細かい模様に縞が出る）、
  仕上げの高品質縮小は既存の経路に任せる。この融合は+1〜2msしか掛からない
- **スレッド数は1本**にする。サムネイル生成は既にファイル単位でコア数ぶん並列なので、
  デコーダ内でも分割すると取り合いになる。原寸表示のときだけ全部使う

**罠**:

- **`irot`/`imir` はデコーダが適用しない**。WICは適用して返すので、HEIC側の
  「適用済みか縦横比で見分ける」判定をこちらへ持ち込むと**掛からないまま**になる。
  同梱デコーダの経路では必ず自分で掛ける（`apply_transform`）
- **色は限定レンジが既定**。full だと思って伸ばすと白飛びする。
  `matrix_coefficients` が2（未指定）のファイルは実際に多く、libavif に合わせて
  **BT.601へ倒す**（BT.709にすると肌の色がずれる）。0（identity）は変換せず
  **G・B・Rがそのまま**入っている
- 正しさは ffmpeg（libdav1d）の出力と突き合わせて確かめた。
  identity/フルレンジは**バイト単位で一致**、4:2:0系は50〜56dB
  （差は色差の補間方式だけで、レンジと係数の判定は一致している）
- **`clap` は寸法の答えにも効かせる**。DBへ書く寸法を `ispe` のままにすると、
  切り出し済みのサムネイルと縦横比が食い違って一覧のタイルが歪む
  （`HeifInfo::crop` を持たせて `display_size` から返す）
- `iloc` の**幅は4ビットの生の値**なので、壊れたファイルは 15 と書ける。
  そのまま読むと9バイト目で桁が溢れるため、規格が許す 0/4/8 以外は読まない
- rav1d から受け取った1枚は、**こちらで使わないと決めた場合でも必ず返す**
  （壊れたAVIFが並んでいると走査のあいだ積み上がる）
- **grid（タイル分割）**は実サンプルが手に入らないので、
  実画素のタイルから2x2のgridを組み立てて確認した。4象限すべて基準の絵と一致

### 段階G-6b: AVIFの積み残し（色の素性・HDR・透過）

段階G-6で割り切った3点を片付けた。

**`colr` を最優先にする**: 規格では**コンテナの `colr` がAV1のシーケンスヘッダより優先**。
これを見ずにシーケンスヘッダだけ信じると、食い違うファイルで色がずれる。
`nclx` だけ読み、ICCプロファイル（`rICC`/`prof`）は
カラーマネジメントが要るので手を出さない。

**HDR（PQ / HLG）をSDRへ直す**: そのまま8ビットにすると**全体が眠くなる**。手順は
①信号を明るさへ戻す ②**拡散白**が1.0になるよう正規化（PQは203ニト、HLGは信号75%。
BT.2408）③原色が広ければsRGBへ移す ④**1.0を超えるハイライトだけ**滑らかに畳む
（中間調は素通しなので普通の写真の見え方が変わらない）⑤sRGBで8ビットへ。

- 検証: SDRの原画→PQ/HLGへ変換→こちらで戻す、の往復で **PSNR 29.8dB**。
  最初に測ったとき15dBしか出ず実装を疑ったが、原因は**素材の作り方**だった
  （SDR白を100ニトに置いた素材）。203ニトで作り直したら期待どおりになった
- **罠**: 原色は **BT.2020 と Display P3 で行列が違う**。同じものを当てると
  鮮やかな色が目に見えてずれる（AppleのHDR写真はP3が多い）
- **罠**: 変換にはべき乗が何度も要り、**毎画素で計算すると桁違いに遅い**
  （190万画素で271ms＝同じ枚数のSDRの10倍以上）。入り口の信号は8ビットに
  揃えてあるので256通りを表にでき、出口は**平方根で刻んだ**表にして暗部の刻みを保つ。
  **271ms → 62ms**。ただし表を**切り捨てで引くとPSNRが1.3dB落ちる**
  （PQは傾きが急なので1段の差が効く）。掛け算1回の補間で戻る

**罠がもう2つ**（レビュー指摘）:

- **`colr` は同じ名前で複数置ける**（規格は色の種類ごとに1つずつ許す）。
  しかも **libavifはICCプロファイルの方を先に書く**ので、名前で1つ目を取ると
  ICCを持つファイルだけ色の指定が丸ごと消え、シーケンスヘッダに落ちてしまう。
  **nclx として読める方**を選ぶこと
- **可逆（`--lossless`）は係数が0**＝「変換しない」になる。ここで早々に返すと、
  可逆のHDRがPQ/HLGのまま出て眠い絵になる。変換しない指定でもHDRの直しは通す

**畳み始めの位置**: 拡散白（1.0）ちょうどに置くと**それより上が全部真っ白に潰れる**。
かといって早く畳むと拡散白そのものが暗くなり、**SDRの写真と並べたとき白がくすむ**。
0.9から畳むと拡散白は8ビットで251に収まり、1.4倍程度までのハイライトに階調が残る。

**透過（alpha）**: 主画像とは別のAV1ストリーム（白黒1枚）が `auxl` で紐づいている。
**白に重ねて**不透明の絵にする（一覧の背景が白なので市松模様より素直）。

- **罠**: `prem`（既に色へalphaが掛けてある）は **`auxl` と向きが逆**で、
  主画像からalphaへ張られる。逆に引くと常に「掛かっていない」と判定し、
  掛け済みの色にもう一度掛けて**半透明の部分だけ薄くなる**
- **罠**: 縮小するときは**透過も色と同じ範囲を平均する**。左上1点だけ見ると、
  細かい透過の縁が「全部不透明」か「全部透明」に倒れてギザギザになる

**検証の作り方**: 透過も `grid` も実サンプルが手に入らないので、
**本物のAV1ストリームを合成コンテナに詰めて**確かめた
（左半分だけ透明なマスクを作り、透明側が純白・不透明側が原色と
バイト単位で一致することを確認）。パーサ側は合成コンテナのユニットテストが本命。

### 段階G-6c: 巨大な画像と、寸法だけ巨大なファイル

8K動画のコマや**1億画素を超える静止画**が実在するので、限界を測った。

| | 展開時間（一覧用・1スレッド） | 最大メモリ |
|---|---|---|
| 1200万画素 | 112ms | 86MB |
| 8K（3300万画素） | 217ms | 224MB |
| **1億800万画素**（12000x9000） | **671ms** | **703MB** |

**素直に比例する**ので、実在する最大級の写真は問題なく通る。危ないのは大きさではなく、
**小さいファイルが巨大な寸法を名乗る**場合（展開爆弾）。AV1は65535四方まで名乗れるので、
この比率だと**28GBを要求されて確保に失敗し、プロセスごと落ちる**
（Rustは確保に失敗すると異常終了する。握り潰せない）。

**対処**: 展開してよい画素数に上限を置く（16384四方＝約2.7億画素。
1億画素級の実物が2倍以上の余裕で通る）。掛ける先は2か所:

- デコーダ側（`frame_size_limit`）——AV1のシーケンスヘッダが名乗る寸法
- こちら側の升目——`grid` は**コンテナが寸法を名乗る**のでデコーダの上限では守れない

あわせて `grid` の**タイル全体**の読み込み量にも上限を置く
（1枚ずつの上限だけでは、タイルを数万枚宣言されると総量が青天井）。

**実測での確認**: 65535四方を名乗る40KBのファイルで、
最大メモリ**4MB**・原寸は拒否・一覧は破綻せずに枠だけ作る、という挙動になった。

**デコーダが無いことの告知**: HEICをOSに委ねる以上、拡張機能が入っていない環境では
サムネイルが付かない。黙って空欄にせず、**ライブラリにHEICが1枚でもあるときだけ**
案内を出す（`decoder_status` → 見本を実際に展開してみて判定。クラウドのみの
ファイルは見本に選ばない＝確認のためにダウンロードを始めない）。

**罠**（レビュー指摘）:
- **試せなかったときは「駄目」ではなく「分からない」に倒す**。見本の候補が
  全部クラウドのみだと1枚も試せないが、そこで偽にすると**嘘の警告**が出る。
  そして**これは例外的な環境ではない**——iPhoneで撮ってクラウド経由で同期すれば
  自然にそうなる（実測したライブラリもHEICの94%がクラウドのみ）。
  つまりHEICを持っている人の多数派がこの経路を通るので、候補は多めに取る
- **起動時に1回だけ見ない**。取り込みやルート追加でHEICが増えるので、
  そのたびに確かめ直す（最初は空だったから黙る、では次の起動まで案内が出ない）
- **導線の無いOSでボタンを出さない**。Storeのページを開いてもmacOS/Linuxでは
  何も入らない。macOSはImageIOが最初から読めるので案内自体が要らず、
  Linuxは配布元のパッケージ（libheif）の話になる

**積み残し**: 候補が全部クラウドのみの環境では判定を保留するので、案内が出ない。
実際に展開へ失敗した事実を覚えておいて知らせる形にすれば埋まるが、
そこまでの作りは「クラウドのタイルにバッジ」と一緒にやる方が筋が良い。

### 未着手（第7部の残り）
- **クラウドのみのファイルのサムネイル**: Windowsは Shell のサムネイル取得
  （`IShellItemImageFactory`）なら**本体をハイドレートせずに**縮小画像が取れる見込み
  （エクスプローラがクラウドのみファイルのサムネイルを出せるのはこの経路）。
  これを可視要求パスに使えば、スクロールしただけで数百枚落ちる問題も消える。
  **動画のサムネイルも同じ経路で片付く**見込みで、こちらはAVIFとは別件
  （rav1dが解けるのはAV1だけで、実際の動画はほぼH.264/HEVC）
- **クラウドのタイルにバッジ**を出す（「まだ取り寄せていない」と伝わるように）。
  あわせて、可視要求で撮影日が確定したときに**タイルが別の日付へ移る**のを
  どう見せるか（自動パスでは mtime 基準で並んでいるため）
- **HEICの穴をOSの範囲で塞ぐ**（デコーダは配らない方針のまま）:
  macOSは **ImageIOが追加インストール不要**（10.13以降）なので実装するだけで閉じる。
  Linuxは **libheif を実行時に `dlopen`**（同梱しない）すれば
  Debian/Ubuntu/Arch はそのまま動く。残るのはHEVCを外しているFedora系だけで、
  そこは配布側の判断なのでアプリでは埋めない
- **`blit` のSIMD化（測ってから）**: 12MP・1スレッドの内訳は
  デコード約60ms（rav1dのasm＝**既にSIMD**）＋画素の変換約50ms（**素のスカラー**）。
  変換を3倍にできても全体は112→75msの1.5倍なので、優先度は高くない。
  安定版で書くなら `wide` クレート（Cライブラリは増えない）。
  **GPGPUは見送り**: 1枚あたりの転送往復が変換時間と同オーダーで割に合わず、
  依存も重く、**WebViewと同じGPUを取り合って一覧が引っかかる**恐れがある。
  加えて**GPUは使い始めに温まるまでの待ちがある**（desktop-touchでも実際に出る）。
  サムネイル生成は**断続的**——スクロールが止まればすぐ暇になる——ので、
  そのたびに待ちを払い直すことになり、この使い方とは相性が最悪。
  そもそもサムネイル生成はファイル単位で既に並列なので、
  改善するのは主に「1枚を今すぐ出す」ときの待ち時間

---
## 【第8部：サムネイル生成の高速化（SIMDと間引き展開）】✅ **実装済み（2026-08-12）**

「可視領域の要求から高品質サムネイルが出るまで」を **147.9ms → 59.3ms（2.49倍）** にした。
実測は Ryzen 7 5700X（Zen 3・AVX2まで）、実写JPEG 40枚（平均19.7MP）の1枚あたり平均。
`cargo run --release --bin bench -- --pipeline <フォルダ> --size 512` で再現できる。

### 測ってから決めた（順番が肝）
最初に段階別ベンチ（`--thumb-dir`）を作り、どこに時間が乗っているかを見た。

| 段階 | 改修前 | 改修後 |
|---|---|---|
| 展開 | 96.0ms | 45.3ms |
| 事前縮小 | 25.6ms | （不要になった） |
| 仕上げ縮小 | 12.5ms | 1.1ms |
| WebPエンコード | 12.6ms | 12.6ms |

**縮小より展開が重い**——これが分かってから手を付ける順番が決まった。

### やったこと1: 縮小をSIMDにする（`fast_image_resize`）
`image` クレートの `imageops` は素のスカラー。`fast_image_resize` は
**SSE4.1/AVX2/NEON を実行時に選ぶ**ので、配布バイナリは素の x86-64 のままでよい
（`-C target-cpu` を上げると古いCPUで起動できなくなる。それはしない）。

- 縮小だけで **4.8倍**（38.1ms → 8.0ms）
- 画質差は現行比 **0.77/255**（平均絶対差）。見分けはつかない
- `crates/pictkura-core/src/resize.rs` に集約し、`thumbnail()`/`resize()` の
  呼び出しを全部こちらへ寄せた（一覧・取り込みウィザード・TIFF配信）

**罠**: `fast_image_resize` は**色にalphaを掛けてから縮め、後で割り戻す**
（透明部分の色が縁ににじまない正しいやり方）。そのため完全に透明な画素は
色が0＝**黒**で戻ってくる。サムネイルは最後に `to_rgb8()` でalphaを捨てるので、
**透明PNGのサムネイルが真っ黒になる**。alphaは捨てずに**白へ重ねる**ようにした
（`resize::flatten_onto_white`。AVIFの `composite_over_white` と同じ考え方）。
ついでに「透明部分の色が不透明側ににじむ」旧実装の滲みも消えた。

### やったこと2: JPEGを「間引きながら」展開する（`mozjpeg`）
512pxのサムネイルを作るのに 19MP を全部起こしていた。JPEGは8x8ブロックを
周波数で持っているので、**低い周波数だけ逆変換すれば 1/8 の絵がそのまま出る**。

デコーダの比較（19MP・40枚の平均）:

| デコーダ | 時間 |
|---|---|
| zune-jpeg（`image` の中身・原寸） | 92.4ms |
| jpeg-decoder（純Rust・原寸） | 134.4ms |
| jpeg-decoder（1/8で間引き） | 93.1ms |
| mozjpeg（原寸） | 84.0ms |
| **mozjpeg（1/8で間引き）** | **40.4ms** |

純Rustの `jpeg-decoder` でも間引きはできるが、1画素あたりが遅くて相殺され、
現行と並ぶだけだった（134→93ms）。**残りはハフマン復号で、間引いても減らない**
——ここが速い libjpeg-turbo でないと勝てない、というのが実測から出た結論。

`mozjpeg` は **ビルドにNASMが要る**が、rav1dのasmで既に要求しているので増えない。
ライセンスはIJG（BSD相当）でMIT配布と両立する。libwebpという前例もある。

つまみ（`do_fancy_upsampling(false)` / `DctMethod::IntegerFast`）は
1/8展開では **差が出なかった**（40.8ms 対 40.2ms）ので、画質の良い既定のままにした。

副産物として、**中間の縮小が1段減ったぶん結果はむしろ素直になった**
（現行比の平均絶対差 0.77 → 0.49/255）。

**罠**: mozjpeg は libjpeg の `longjmp` を `resume_unwind`（＝パニック）に
変換して寄こす。クレート自身が「壊れたファイルではパニックする。Resultは
ほぼ役に立たない」と断っている。写真ライブラリには壊れたJPEGが混ざるもので、
そのままではサムネイルのワーカースレッドが落ちる。`catch_unwind` で受け止めて
`None` に均し、呼び出し側は `image::open` へ戻る（`jpeg.rs::catching`）。
`resume_unwind` はパニックハンドラを通らないので**コンソールに何も出ない**
——テストが「メッセージ無しで失敗する」形で気付いた。

### やらないと決めたこと: `blit` のSIMD化
第7部で「測ってから」としていた宿題。測った結果 **やらない**。

12MP AVIF・一覧用（512px・1スレッド）の内訳:
- `decode_tile`: **85.2ms**（rav1dのasm＝既にSIMD）
- `blit`: **11.4ms**

第7部に書いた「デコード60ms＋変換50ms」は**原寸経路の数字**だった
（原寸・全スレッドなら decode 22.8ms + blit 67.2ms）。
本番は原寸経路を呼んでいない——`source_image` は常に `Some(thumb_size)` を渡す。
一覧用でのblitは全体の11%しかなく、3倍にできても 99ms → 92ms。
しかもAVIF限定で、実ライブラリのAVIFはほぼ0枚。手を入れる価値が無い。

### 次に効きそうなところ（未着手）
- **1枚を今すぐ出すときのAVIF**: `decode_tile` は1スレッドで85.2ms、
  全スレッドなら22.8ms（**3.7倍**）。ファイル単位の並列と取り合うので
  常時そうはできないが、**可視要求（優先キュー）の1枚だけ**スレッドを増やす
  余地がある。スクロールが止まった直後の待ち時間に効く
- **残った44.8ms**はほぼハフマン復号で、SIMDでは減らない。減らすなら
  「そもそも展開しない」——EXIF埋め込みサムネイルが512pxに足りる機種では
  即席をそのまま高品質として採用してよいはず（今は必ず作り直している）
- **WebPエンコード12.2ms**が展開の次に重い。品質82の据え置きで
  `method` を下げると速くなるが、容量とのトレードオフを測ってから

---
## 【第9部：動画対応】✅ **実装済み（2026-08-13）**

**画素を自前でデコードしない**——RAW・HEIFで決めた線をそのまま動画へ延ばす。
一覧の絵はOSのサムネイル機構から借り、再生はWebViewに任せる。

### 済んだところ
- `video.rs`: コンテナ（ISO-BMFF）から**寸法・長さ・撮影日時**を読む。
  `moov` だけを拾うので**画素は1バイトも読まない**（実測0.2ms／iPhoneの.MOV）。
  縦持ちは `tkhd` の回転行列を見て寸法を入れ替える（1920x1080 → 1080x1920）
- `shell_thumb.rs`: WindowsのShell（`IShellItemImageFactory`）から1コマ借りる。
  実測 190ms（初回）／29ms（Windowsのサムネイルキャッシュに載った後）。
  **罠**: `canonicalize` が付ける拡張長プレフィックスを剥がさないと
  `SHCreateItemFromParsingName` が 0x80070057 で弾く。
  .avi でも絵が出ることを確認（2026-08-13。合成したMJPEG/AVIで実測）。
  .m2ts は手元の該当ファイルがクラウドのみで**未確認のまま**
- 走査対象の拡張子に動画を追加。`process_one` に動画の枝を足した
- DBに `duration_ms` 列

### 決めたこと: HEVCのデコーダは配らない
iPhoneの動画は**HEVC**（実測: サンプルの.MOVは全部 `hvc1`）。
再生できるかはOSのデコーダ次第で、**HEICと同じ穴**にそのまま乗る。

WebView2での実測（Edge 151・HEVC拡張機能あり）:

| 項目 | 結果 |
|---|---|
| `canPlayType('video/mp4; codecs="hvc1"')` | **空（不可と申告）** |
| 実ファイルの再生 | **成功。24フレームをデコード・取りこぼし0** |

**`canPlayType` は当てにならない**。空を返しておきながらOSのデコーダ経由で
普通に再生する。対応可否の判定に使ってはいけない。

配布方針は「**HEVCのデコーダは配らない。OSが持っていれば使う**」で統一する:
Windows = Storeの拡張機能 / macOS = VideoToolbox / Linux = ディストリのlibavcodec。
特許プールが課金対象と見るのは**デコーダを配る行為**なので、配らなければ当事者にならない。
「無償OSSだから」「利用者の責任」といった宣言では移らない
（Fedoraが外しDebianが入れているのは、解釈の違いではなくリスクの引き受け方の違い）。

**案内は無料と有料を分けた**（2026-08-13）:
- 無料: HEIF Image Extensions（`9pmmsr1cgpwg`）— HEIFの**コンテナ**
- 有料: HEVC Video Extensions（`9nmzlz57r3t7`・数百円）— HEVCの**画素**

`open_decoder_help(kind)` で開き分ける。HEICの案内も
「無料の拡張機能に加えて有料のHEVC拡張機能が要る」と書き、ボタンを2つ出す
（**HEIC画像でも画素はHEVC**なので有料側が要る。みんなが買うとは限らないので、
どこまで無料で済むかが読んで分かる文言にした）。

### 分かっている穴（直さずに記録した分）
- **`mvhd` の時刻は現地時刻で書かれていることがある**。規格上はUTCなので
  UTCとして換算しているが、Canon/Sony/Panasonic の .MP4/.MOV は
  **現地時刻を書いたうえで `udta` の日付を持たない**ことが多い。
  `day_key` はローカル日付で作るので、20:00 JST の動画が翌日へ回りうる。
  mtimeとの差が時差ぶんに近いかで推測する手はあるが、mtime自体が
  「カードからコピーした日」のこともあり当てにならない。**要判断**として残す
  （iPhoneは `udta` を持つので、手元のライブラリの大半は正しく出る）
- **Windows以外では動画のサムネイルが1枚も出ない**。`shell_thumb::thumbnail`
  が常に `None` を返し、可視要求ごとに `Err` になる（セッション内3回で打ち切り）。
  macOS/Linux の枝を足すまでは、動画は枠だけが並ぶ

### 再生（2026-08-13）
- **`media://video/<id>` を新設し、Rangeリクエストに応える**。`<video>` は
  シーク・先読みのたびに部分要求を出すので、206を返せないと再生が成立しない
  （Chromiumはシークを諦める）。**要求より少なく返してよい**のがRangeの規則なので
  **4MiBずつ刻む**——これで2GBの.m2tsでも常駐は4MiB。判断は
  `protocol::plan_range`（純関数）に出してユニットテストで固めた
- 絵の経路（`thumb`/`full`）は今までどおり原本を丸ごと読むが、**動画はそこに
  入れない**。`<img>` に動画を渡しても絵にならず、丸読みの害だけが残る
- 一覧に**▶バッジと長さ**（`0:45` / 1時間超は `1:02:03`）。長さは
  `duration_ms`（コンテナ読み）そのままで、動画を開かない
- ビューアは動画なら `<video controls autoplay>`。操作バーは**ブラウザ既定に任せる**
  （自前で作ると全画面・音量・字幕まで面倒を見ることになる）
- **スペースは再生/一時停止**に振り替える（写真のときはスライドショー）。
  ←→は**動画でも前後の写真へ移動**のまま——ビューア全体で意味を変えない方が
  迷わない。シークは操作バーで行う（Windowsフォトも同じ挙動）
- 再生できないときの逃げ道を2種類に分けた:
  - **コンテナが違う**（.m2ts/.avi）… `plays_in_webview` が偽。415を返し、
    UIは「既定のアプリで開く」だけを出す（コーデックの話ではないので拡張機能の
    案内はしない）
  - **コーデックが無い**（HEVC）… `<video>` の `error` を拾ってから出す。
    `canPlayType` は当てにならない（空を返しながら再生できる）ので**事前判定しない**。
    ここでは有料のHEVC拡張機能への導線も出す

**罠: CSPの `media-src`**。`img-src` に `http://media.localhost` を許可していても、
`<video>` は `media-src` を見る（未指定なら `default-src 'self'` にフォールバック）。
指定を足すまで**動画だけが無言で `error` になり**、コーデック不足と見分けが付かなかった。

実測（2026-08-13・合成した54MBのH.264 MP4／45秒）:

| 確かめたこと | 結果 |
|---|---|
| 連続再生（4MiBを繰り返し要求） | 26秒過ぎまで途切れず再生 |
| 操作バーでシーク | 5秒地点から33.7秒へ即座に飛ぶ（ファイル中央の206） |
| iPhoneのHEVC .MOV | 再生できた（OSのデコーダ経由。縦持ちも正しい向き） |
| MJPEGの.avi | 「既定のアプリで開く」だけの案内が出る |

### 2ゲートレビューで直したところ（2026-08-13）
- **クラウドにしか実体が無い動画は開く前に止める**（一番効いた指摘）。
  4MiBを読むだけでもハイドレートは**ファイル全体**に走るので、数GBの動画なら
  何分も黒い枠のまま固まる。ビューアで動画を開いた**1回だけ** `video_status` を
  聞き（属性を見るだけなのでダウンロードは起きない）、クラウドなら
  「既定のアプリで開く」へ寄せる案内を出す。配信側でも503を返して守る。
  実測: 45MBのクラウドのみ.MOVを開いても**ハイドレートされないまま**案内が出た
- **「再生できません」の理由を取り違えない**。`onError` を全部
  「HEVCが無い」と解釈していたので、ファイルを消しただけで**有料の拡張機能を
  勧めてしまう**。`video_status` の `exists` / `cloud_only` / `plays_in_app` で
  先に切り分け、拡張機能の案内は「コンテナは扱えるのに再生できなかった」ときだけ出す
- **読めないRangeヘッダは無視する**（RFC 9110）。`items=0-10` や `bytes=abc` を
  416で突き返していた。「Rangeを付けたばかりにファイルが取れない」のは規格違反。
  416にするのは**形は正しいが範囲外**のときだけ（`parse_range` を3値に）
- **読み出しが足りなければ404**。`Content-Range` は読む前のファイル長で書くので、
  途中でファイルが縮む（クラウドの同期・書き出しでの上書き）と長さが食い違い、
  WebViewにはプロトコル違反として見える
- **スライドショーは動画の間は送らない**。3秒で切り替わっては見られないうえ、
  スペースは動画の再生/一時停止に取られているので止める手が無くなっていた。
  動画が終わったら次へ進む（再生できない動画は絵が出ないので今までどおり3秒）

### 段階H: 素性をOSから借りる（クラウドのみ・自前で読めないコンテナ）
**実装済み（2026-08-13）**

第9部の積み残しを潰すつもりで測ったら、**動画より大きい穴が写真側に空いていた**。
どちらも原因は同じ「ファイルを開かないと素性が読めない」ことで、
**Windowsのプロパティ（`IPropertyStore`）を使うと開かずに読める**。

測って分かった穴（実測 2026-08-13、ライブラリ6,684件）:

| 相手 | 件数 | 寸法・撮影日時・長さ |
|---|---|---|
| 実体のある動画 | 93 | 全部取れている |
| **クラウドのみの動画** | **146（動画の61%）** | **全部空** |
| **クラウドのみのHEIC** | **3,020（HEICの83%）** | **全部空** |
| `.m2ts` / `.avi` | — | **絵は出るのに空**（コンテナが対象外） |

症状はどれも同じで、一覧の並びが `COALESCE(taken_at_ms, mtime_ms)` の
mtime に落ち、▶バッジの横に長さが出ない。`thumbs.rs` のクラウドの門にも
「これを消すにはファイルを開かずに素性を得る経路が要る」と書き残してあった。

**やったこと**: `shell_thumb.rs` を `shell.rs` に改め（絵だけでなく素性も借りるので）、
`shell::metadata` を足した。`SHGetPropertyStoreFromParsingName` で
`System.Video.FrameWidth/FrameHeight`・`System.Image.HorizontalSize/VerticalSize`・
`System.Photo.DateTaken`・`System.Media.DateEncoded`・`System.Media.Duration` を読む。
呼ぶのは2か所だけ:

- **クラウドの門**（`process_one`）… 絵は諦める前に素性だけ貰う。`Db::update_shell_metadata`
  で書く（`update_metadata` と違い **`camera_id` に触らない**。Shellはカメラ名を返さないので、
  ここで「確認済み・情報なし」を立てると実物を読める日が来ても読み直さなくなる）
- **`process_video`** … コンテナ読みが空を返した項目だけ補う

**実測（走査し直し）**: 未確定3,166件が**20秒で1件**まで減り、
クラウドのみの動画146本は**146本すべてで寸法と撮影日時が埋まった**（うち111本は長さも）。
**クラウドのみのファイルは146本のまま＝1バイトも落ちていない**。
日時も合っている（`20191218_025219000_iOS.MOV`＝UTC 02:52:19 → DB 11:52:19 JST）。
`.m2ts` は 1920x1080・10秒、`.avi` は 640x480・5秒が入った。
残る1件はOneDrive側にキャッシュが無いHEICで、開いた時に埋まる。

**罠を4つ踏んだ**:

- **`GPS_FASTPROPERTIESONLY` は使えない**。「ディスクを一切読まない」指定なので、
  安全側に倒すつもりで指定すると**どのファイルでも何も返らない**。`GPS_DEFAULT` を使う
- **ハイドレートしない根拠**は「長さだけが返らない」こと。長さはコンテナを解釈する
  プロパティハンドラしか出せないので、返らない＝**ハンドラが動いていない＝
  ファイルが開かれていない**。返るのは同期クライアントが置いた分だけで、
  問い合わせ前後でファイル属性も変わらない（`bench --shell-meta` で確認できる）
- **持っていないキーでも `GetValue` は成功する**（`VT_EMPTY`）。`PropVariantToUInt32` は
  それを**0に変換して成功**させるので、素直に受け取ると「取れた（0）」になり、
  次のキーを試す `or_else` へ進まない。実際にこれでクラウドのみの動画の寸法が落ちた
- **`System.Video.Frame*` は回転前の格納寸法**。縦持ちの動画では幅と高さが逆になる。
  ここが使われるのは自前のコンテナ読みが失敗したとき（`.m2ts` / `.avi`）だけで、
  そのコンテナには回転情報自体が無いので実害は出ない。クラウドのみのファイルでは
  `System.Image.*` の側が返り、そちらは表示どおりの向きで入っている

**`PKEY_*` は windows クレートに入っていない**ので自前で書いた。綴りを間違えても
「そのプロパティが無い」ようにしか見えず失敗が失敗として現れないので、
`PSGetPropertyKeyFromName` と突き合わせるテストで固定した。

`ids_missing_metadata` は `width = 0` も拾うようにした。0は「読もうとして読めなかった」印で、
旧版で走査済みの `.m2ts` / `.avi` がそこに止まっていたため。

**調査用**: `bench --shell-meta <ファイル|フォルダ>`（取れた素性＋前後のファイル属性）、
`bench --shell-dump <ファイル>`（Shellが持っているプロパティを正式名で全部）。

### 段階H-2: EXIFが無い画像の日付をファイル名から拾う
**実装済み（2026-08-13）**

段階Hで「撮影日時が空」は6,685件中1件まで減ったが、**空ではないが嘘**という行が
残っていた。EXIFもOSのプロパティも持たない保存画像（LINE・スクリーンショットの類）は
mtime に落ちるので、**213件が OneDrive の同期日 2025-07-07 に並んでいた**。
`20220121_020918000_iOS.jpg` のように、**ファイル名だけが正しい日付を知っている**。

**測ってから決めた**（実測 2026-08-13）:

| 手段 | カバー | 判断 |
|---|---|---|
| **ファイル名の日付** | 5,613件（83%） | 別途日付が分かっている5,276件と96%一致＝信用できる。**採用** |
| 親フォルダ名の日付 | 236件 | 問題の213件は `カメラ ロール/2022/01` にあり日付フォルダではない。**見送り** |
| mtimeと作成日時の小さい方 | ほぼ0 | OneDriveは両方を同期日にするので効かない。**見送り** |
| 近傍ファイルからの推測 | — | 撮影日を捏造することになる。**やらない** |

**時間帯の扱いが肝**。同じ `20220121_020918` でも、OneDriveのカメラロール
（`_iOS` で終わる命名）は**UTC**、Android やスクリーンショットの命名は**現地時刻**。
混ぜると時差ぶんずれる。日付が別途分かっている行で突き合わせたところ、
**`_iOS` 命名 5,146件のうち 5,145件（99.98%）がUTC**だった
（残り1件は撮影地の時間帯が違うと思われる）。他の命名はこのライブラリに実物が無く、
慣習どおり現地時刻として扱う。

**日付だけの名前は採らない**。`IMG-20240101-WA0001` のように時刻を持たない命名も
あるが、0時に決め打つとその日の先頭へ並び「日付は合っているが並びは嘘」になる。
**日付と時刻が揃っている名前だけ**を扱う（`namedate.rs`。正規表現を使わない自前の
走査で、区切り文字は何でもよい）。

順番は **EXIF → OSのプロパティ → ファイル名 → mtime**。呼ぶのは4か所
（画像・動画・クラウドの門・取り込み先フォルダの決定）。取り込み側も直したのは、
ここを mtime へ落とすとコピー先フォルダが「取り込んだ日」になり、
一覧の表示日とずれるため。

**既存DBの拾い直し**: `taken_at_ms = mtime_ms` の行（実測965件）を一度だけ
キューへ投げ直す（`namedate_sweep` = v1）。**DBを直接書き換えない**のが要点で、
投げ直せばEXIFが読めるファイルはEXIFが勝ち、名前で上書きされることはない。
実測: 起動して**ずれていた213件が0件**になり、クラウドのみの3,166件は落ちないまま。

### mvhd の現地時刻問題: 直さないと決めた（2026-08-13）
「Canon/Sony/Panasonic は `mvhd` に現地時刻を書く」という懸念で保留していた件。
**手元で唯一のカメラ由来ファイルで検証したところ、UTCとして扱うのが正しかった。**

Canonの `MDG_nnnn.MP4`（`compatible_brands` に `CAEP`、`udta` を持たず `mvhd` のみ）を
UTCとして換算すると 13:06:37。同じフォルダの連番JPEGが
`IMG_(n-1)`=13:05:50 → **動画=13:06:37** → `IMG_(n+1)`=13:06:37 → `IMG_(n+2)`=13:06:44 と、
**秒単位で列に挟まる**。現地時刻が書かれていたなら9時間ずれて早朝4時へ飛ぶ。
mtime（13:07:00＝録画10秒＋書き込み）とも合う。Shellも同じ値を返す＝**Windowsも同じ解釈**。

ライブラリ239本の `taken_at - mtime` の分布にも**時差ぶん（9時間）の山は無い**
（iPhoneは `udta` を持つので正しく出る）。

**直さない理由**: 補正の判定式は書ける——`udta` が無く、
`mtime - (mvhd_utc + 長さ)` が**ちょうど時差ぶん**なら現地時刻とみなして戻す
（ほぼ0なら触らない）。上のCanonのファイルでは差が23秒なので正しく素通しする。
ただし**手元に「現地時刻を書いたファイル」が1つも無い**ので、直せたかを確かめられない。
外すと**9時間ずらす**副作用が出る種類の補正を、検証できないまま入れるのは割に合わない。
現地時刻を書く実物が手に入ったら、上の判定式で入れる。

### 2ゲートレビューで直したところ（2026-08-13・段階H / H-2）
- **クラウドのみをカメラ補完から外す**（Codex P1・一番効いた指摘）。
  カメラの後追い補完は `camera_id IS NULL AND width IS NOT NULL` で対象を選ぶので、
  段階Hでクラウドのみのファイルに `width` が入った結果、**3,165件が丸ごとこの列に
  並んだ**。その先の `read_exif_info` はファイルを開くため、**次回起動で
  ライブラリ全体がダウンロードされる**ところだった。未マウントのドライブと
  同じ扱いにして、印を付けずに飛ばす
- **一度きりの拾い直しに「済んだ印」を持たせない**（Codex P1）。印を先に書くと、
  投げた仕事が終わる前にアプリが落ちたときに二度と拾い直せない。代わりに
  **投げ直す意味のある行だけ**に絞った（名前から拾える日時が mtime と違う行）。
  直った行は条件から外れ、直らない行はファイルを開かない文字列判定で弾ける
- **名前の日時が mtime と同じ行を投げ直さない**（/code-review）。スクリーンショットの
  ように名前とmtimeが同じ秒のファイルは、投げ直しても値が変わらず条件から外れないので、
  同期のたびに永久に回る。**RAW/HEIFはその都度原寸を展開し直す**ので実害が大きい。
  実測: 修正前131件が毎回投げ直されていた → 修正後0件
- **画像もOSのプロパティを見る**（/code-review）。XMPにしか撮影日を持たない書き出し
  （Lightroom・Google フォト）は `read_exif` が何も返さないが、Shellのハンドラは読める。
  ここを飛ばすと、背景の門がOSから正しい日付を入れた後で、**可視要求の経路が
  mtime で上書きし直す**（タイルが同期日へ飛ぶ）
- **撮影日時がNULLの行も拾い直しの対象にする**（/code-review）。`taken_at_ms = mtime_ms`
  はSQLのNULL比較で外れるため、段階Hで**寸法だけ埋まった行**（OSが寸法は返したが
  日付は返さなかった）が、`ids_missing_metadata`（`width IS NULL OR width = 0`）からも
  漏れて両方の網から落ちていた
- **一度落ちてきて戻されたファイルもクラウドの門で拾い直す**（/code-review）。
  `taken_at_ms == mtime_ms` も「読めなくて落ちた」印として門の条件に足した
- **`_iOS-1` を現地時刻と誤判定しない**（Codex P2）。取り込みの衝突リネームで
  連番が付くと語尾の判定が外れ、時差ぶんずれた日に並ぶ
- 存在しない `Depth::Fast` への言及を消した（/code-review）。API整理で消した名前が
  コメントに残り、「フラグが非ハイドレートを保証する」と読めてしまう。
  実際の根拠は「長さだけが返らない＝ハンドラが動いていない」という実測の側にある

### 残っているところ
- **macOS / Linux のサムネイル**: QuickLook / ディストリのサムネイラ。
  `shell_thumb::thumbnail` が唯一の入口なので、ここに枝を足すだけで済む形にしてある
- **ffmpegの是非**: 同梱すれば .m2ts/.avi のアプリ内再生とLinuxのサムネイルが埋まる。
  ただし m2ts は H.264 が MPEG-TS に入っているだけなので、**再エンコードせず
  コンテナを詰め替える（remux）だけで再生できる**見込み。デコーダを通さずに済む。
  入れる場合も `--disable-decoder=hevc --disable-encoders` で
  「HEVCデコーダを配らない」線は守る

---
## 【v0.1（最初の配布）】✅ **ひととおり完了（2026-08-13）**

機能追加はここで止め、**今あるものを配れる形にする**のが v0.1 の定義。

- [x] **配布形式**: `bundle.active` を有効化。**MSI（ja-JP / en-US）＋ ポータブルZIP**。
      NSISも試したが、配布形式はMSIとZIPにするとユーザー判断（2026-08-13）。
      ポータブルZIPは `tools/pack-portable.ps1` で作る——中身の並びを
      **インストール後と同じ**にするのが要点で、アプリは実行ファイルの隣から
      取説とライセンス一覧を探すため、崩すと「同梱されていません」になる。
      **ビルドにNASMが要る**（rav1dのasm。無効化すると6倍遅くなるので外さない。
      mozjpeg/libjpeg-turbo も同じNASMを使う）
- [x] **アプリ内のバージョン表示**と「このアプリについて」（設定ダイアログの最下段）。
      **罠**: `tauri.conf.json` の `resources` に `../` を含めて書いたものは、
      資源置き場の直下ではなく **`_up_/` の下**へ集められる。素のパスだけを見ると
      **配布したときだけ**「同梱されていない」と誤判定するので、両方を試す
- [x] **OSSライセンス一覧**: `cargo-about`（`about.toml` / `about.hbs`）＋
      npm用の自前スクリプト（`ui/scripts/licenses.mjs`）で `THIRD-PARTY-LICENSES.txt` を生成。
      **IJGを `accepted` に足す必要がある**（mozjpeg / mozjpeg-sys）。
      rav1d のdav1d由来の著作権表示2件（`VideoLAN and dav1d authors` /
      `Internet Security Research Group`）が入っていることを確認済み
- [x] **LICENSE ファイル**（MIT）
- [x] **取説**: `docs/manual.html`（単体で読める9章立て、ダーク/ライトはOS追従）
- [x] **README刷新**: スクリーンショット4枚・使い方の最短導線・対応形式の表・注意事項
- [x] 既知の穴の整理（README と 取説の両方に表で明記）

**スクリーンショットの撮り方**（再現手順）: 本物の設定・DBに触れずに撮るため、
`tauri.conf.json` の `identifier` を一時的に `dev.harusame.pictkura-demo` へ変えて
ビルドすると、**専用の設定フォルダ**が使われる。`APPDATA` を差し替える手は効かない
（TauriはKnown Folder APIを見るので環境変数を無視する）。写真は Wikimedia Commons の
**CC0 / パブリックドメイン**のみ（`docs/images/SOURCES.tsv`）。カテゴリ検索そのままだと
軍用の暗視画像や書籍スキャンが混ざるので、Unsplash由来のCC0に絞ると素直。
Wikimediaは規約に沿ったUser-Agentと**1〜2秒の間隔**が要る（429で弾かれる）。

**用語**: メディアの入れ物は「**コンテナ**」と書く。以前は「容器」と書いていたが、
コンテナフォーマットの訳語としては前者が標準（2026-08-13に57箇所を統一）。

**残り（配布の前に決めること）**:
- **Dependabot の glib（GHSA-wrw7-89jp-8q8g）**: `>=0.15.0 <0.20.0` が対象で、
  現在 0.18.5。**Windowsビルドには入らない**（Linux専用の依存で、
  gtk 0.18.2 ← tao/muda ← tauri が固定している）。tauri が上げるまで動かせないので据え置き
- **MSIは管理者権限が要る**（per-machine）。管理者になれない人にはポータブルZIPが受け皿

**公開（public化）の段取り（2026-08-13、ユーザー判断）** ✅ **実行済み**:
リリース前に**履歴をリセットし、新しいリポジトリへ単一コミットで push する**。
理由: **GitHubはforce pushしても古いコミットを即座に回収しない**ので、
SHAを直接指定すれば読める。private のうちは見えないが、**public にした瞬間に読める**。
新しいリポジトリなら古いオブジェクトがそもそも存在しない。
実行は**v0.1の作業を積み終えてから**（先にやると後続のコミットがまた積まれる）。
消す前に `git bundle create <外部パス> --all` で履歴を退避しておくこと。

## 【v1 以降（やりたいが v0.1 には入れない）】

「最小機能×爆速」を壊さないため、**配れる形にするまでは手を出さない**もの。

- **任意タグ付け**: ユーザーが自由に付けるタグ。FTS5の索引をタグにも広げる形が素直
  （`media_fts` に列を足すか、タグ専用の転置表を持つか。1000万件でのコストを先に測る）
- **GPSからの住所タグ**: 撮影地の緯度経度を地名に変換して検索できるようにする。
  逆ジオコーディングはオンライン APIかオフラインデータ同梱かで性格が変わる
  （プライバシーと起動速度の観点ではオフライン優先）
- **MCPサーバー機能**: 写真整理やタグ付けをローカルLLMに頼めるようにする。
  pictkura が MCP サーバーとして「検索・タグ付け・整理」の道具を公開する形

---
# 【引き継ぎ】現在地と、次にやること（2026-08-14）

**このファイルが唯一の引き継ぎ資料**。開発機を替えると、対話AIの記憶（メモリ）は
引き継がれないので、環境の準備・運用の約束・踏んだ落とし穴もここに全部書いてある。

## いまどこ

第1〜9部＋段階H/H-2、および **v0.1の配布準備までひととおり完了**。

**公開リポジトリへ移った（2026-08-13）**。段取りは「v0.1」の節に書いたとおりに実行した。

| | |
|---|---|
| 公開 | https://github.com/Harusame64/pictkura （PUBLIC・単一コミットから始まる） |
| 旧リポジトリ | `pictkura-dev`（PRIVATE・59コミットの履歴を保持）＋ ローカルに `git bundle` を退避 |

**CI/CD ができた（2026-08-13〜14）**。「未着手」に挙げていた項目のうち CI がここで埋まった。

| ワークフロー | 中身 |
|---|---|
| `.github/workflows/ci.yml` | push（main）とPRで、**Windows と macOS** の両方で UIビルド → fmt → clippy（`-D warnings`）→ テスト（`fail-fast: false`） |
| `.github/workflows/release.yml` | タグ `v*` の push で、Windowsは MSI 2言語＋ポータブルZIP、macOSは `.app` のZIPを作り、**両方揃ってから** Releases に載せる。手動実行（`workflow_dispatch`）では作るだけ |

`release.yml` は**空撃ちで通ることを実測済み**（2026-08-14、`workflow_dispatch`／11分）。
MSI 8.0MB×2・ポータブルZIP 8.1MB が出て、ZIPの中身（実行ファイル・取説・
ライセンス一覧・画像）の点検も通った。手元の `pwsh tools/release.ps1` は今も同じに動く。

**macOSを足した（2026-08-14）**。詳しくは「macOSも配る」の節。

**公開は `publish` ジョブに切り出した。** 各ビルドジョブが自分で `gh release create`
すると、2つが同時に同じReleaseを作りにいって競り合ううえ、**片方が落ちたときに
片OSだけのリリースが出来てしまう**。両方が揃うのを待ち、配布物が4つ
（MSI×2・ポータブルZIP・macOSのZIP）あることを数えてから1回だけ載せる。
書き込み権を持つのはこのジョブだけで、ビルドジョブは `contents: read` に落とした。

### 決着: MSVC で作ってよい（2026-08-14に実測）

このファイルは長らく `x86_64-pc-windows-gnu` を前提に書いてきたが、
**CIは `windows-latest` の既定＝MSVC でビルドしている**。MSVC はCランタイムを
動的リンクするため「素のWindowsでポータブル版が起動しないのでは」という疑いがあった。
`dumpbin /dependents` で測った結果:

```
bcryptprimitives / advapi32 / ntdll / user32 / kernel32 / comctl32 / gdi32 /
ole32 / shlwapi / combase / shell32 / oleaut32 / dwmapi / propsys /
api-ms-win-core-synch-l1-2-0 /
api-ms-win-crt-{math,string,convert,heap,stdio,utility,runtime,time,locale}-*
```

**`vcruntime140.dll` も `msvcp140.dll` も無い。** Cランタイムは `api-ms-win-crt-*`
（UCRTのAPIセット）経由で、これは**Windows 10以降のOSに同梱**されている。
VC++再頒布可能パッケージは要らない。したがって MSVC で作った配布物で問題ない。

ただし測ったのは `pictkura.exe` の**静的インポート**であって、
「素のWindowsで起動する」ことの検証ではない（WebView2ランタイムが別途要るのは既知）。
手元のGNU環境をMSVCへ移すかどうかは別の判断——**配布物はCIが作るMSVCの方**になる。

## 別のPCで続きをやるときの準備

### 要るもの

| もの | 用途・注意 |
|---|---|
| Rust（`x86_64-pc-windows-gnu`） | 手元はこれで通してきた（cdylib不可なのでrlibのみ）。**CIとリリースはMSVC**で、依存DLLの実測から問題ないことが分かっている（上記） |
| MinGW-w64 の `gcc` | SQLite・libwebp・libjpeg-turbo をソースからビルドする（GNUターゲットの場合） |
| **NASM** | rav1d（AVIF）と libjpeg-turbo のアセンブリ。**外すと6倍遅くなる** |
| Node.js / npm | UIのビルドと、npm側のライセンス一覧の生成 |
| `cargo install tauri-cli --version "^2.0"` | `cargo tauri build`（MSI）に要る。**`--features cli` は不要** |
| `cargo install cargo-about --features cli` | OSSライセンス一覧の生成。**`--features cli` が無いとバイナリが入らない** |
| ffmpeg | 動画の検証用ファイルを合成する（下記） |
| codex CLI（ログイン済み） | 2ゲートレビューのゲート1。**版による制約あり**（「運用の約束」を見ること） |

### 最初にやること

```bash
npm --prefix ui install
cargo test -p pictkura-core          # 276件通ればOK
npm --prefix ui run build
cargo run -p pictkura --release      # debugビルドはサムネ生成が10倍以上遅い
```

設定とDBは `%APPDATA%\dev.harusame.pictkura\` に**新しく作られる**。
ライブラリのフォルダは登録し直しになる。

### 手元に無くなるもの（作り直しが要る）

- **RAWの実サンプル**: raw.pixls.us（CC0）から集めていた22件。
  `PICTKURA_RAW_SAMPLES=<フォルダ>` を指定したときだけ走るオプトインのテストなので、
  無くてもテストは通る（合成TIFFのユニットテストが本命）。
  `bench --raw-dir <フォルダ>` でカバレッジ表を出せる
- **動画の検証コーパス**: ffmpegで合成できる。ライブラリ配下に置いて走査させる

  ```bash
  # H.264 + AAC の長めのMP4（Rangeリクエストの確認用）
  ffmpeg -f lavfi -i "testsrc2=size=1920x1080:rate=30:duration=45" -f lavfi -i "sine=duration=45" \
    -c:v libx264 -pix_fmt yuv420p -c:a aac big_h264.mp4
  # AVCHD相当（BDAVの192バイトパケット、音声はAC-3）
  ffmpeg -i big_h264.mp4 -t 10 -c:v copy -c:a ac3 -f mpegts -mpegts_m2ts_mode 1 avchd.m2ts
  # MJPEGのAVI（アプリ内再生できない容器の確認用）
  ffmpeg -f lavfi -i "testsrc2=size=640x480:rate=25:duration=5" -c:v mjpeg legacy.avi
  ```
- **READMEのスクリーンショット用デモ画像**: Wikimedia Commons の CC0/PD から集めた26枚
  （出典は `docs/images/SOURCES.tsv`）。撮り直しの手順は「v0.1」の節を参照

## すぐ手を付けられる残件

- [ ] **`docs/images/settings.jpg` を撮り直す**。→ **Windows機でやること**（2026-08-15判断）。
      詳しくは下の「スクリーンショットの撮り直しはWindows機で」の節。
      この画像は `tauri.conf.json` の `resources` に入っているので
      **MSI・ポータブルZIP・.app のすべてに同梱される**——README だけの問題ではない。
      いまの画像は**修正前のUI**で、プリセットと「自分で決める」が**両方選択済みに見える**
      ——2ゲートレビューで指摘された不具合そのものが写っている。コードは修正済み。
      **v0.1.0には撮り直さないと決めた**（2026-08-14のユーザー判断）が、v0.1.1でも
      持ち越しになった。配る版が増えるほど古い絵が出回るので、Windows機を触るときに必ず消化する
- [ ] **「爆速」「PlayMemories Home 風」の扱い**（ユーザー判断待ち）。
      README（英日とも）からは外した。**plan.md本文とコードのコメントには残っている**。
      消すかどうか未決
- [ ] **MSIは管理者権限が要る**（per-machine）。管理者になれない人の受け皿は
      ポータブルZIP。NSISなら per-user にできるが、配布形式はMSI+ZIPと決めた
- [x] **Dependabot の glib（GHSA-wrw7-89jp-8q8g）**: 2026-08-13に**dismiss済み**。
      `>=0.15.0 <0.20.0` が対象で現在0.18.5だが、**Windowsビルドの依存グラフに入らない**
      （Linux専用。gtk 0.18.2 ← tao/muda ← tauri 2.11.5＝最新 が固定）。
      pictkuraはglibを直接使っていない。上げる道が無く、配布物にも入らないため。
      **Linuxを配るようになったら判断し直すこと**（そのときはtauriがgtkを
      上げているかを先に見る）。なお新しいリポジトリへ移すと同じ警告が再び出る
      ——`Cargo.lock` が同じなので、そこでも同じ理由でdismissする
- [x] **`release.yml` の空撃ち**: 2026-08-14に実施。配布物の生成と依存DLLの実測が済み、
      MSVC/GNU も決着した（「いまどこ」の節）。**初回のタグを打つ前にやること**という
      2ゲートレビューの助言に従ったもので、実際 `cargo tauri build` と WiX は
      それまでCIで一度も動いていなかった

## スクリーンショットの撮り直しはWindows機で（2026-08-15判断）

`settings.jpg` を macOS の開発機で撮り直そうとして、**途中でやめた**。経緯と、
次にやる人が同じ道を辿らなくて済むように分かったことを残す。

**やめた理由: 4枚とも Windows 版の画面だから。** `grid.jpg` / `import.jpg` /
`settings.jpg` / `viewer.jpg` のどれにも左ペインが写っていて、そこに
`Windows (C:)` `ボリューム (D:)` と「フォルダを追加」の例示 `例: D:\Pictures` が
入っている。macOS で1枚だけ撮り直すと、**その1枚だけドライブ一覧の見た目が違う**。
4枚とも macOS で撮り直す手もあるが、主力は依然 Windows 版（macOS版は
機能が3つ欠けている）なので、READMEの絵を全部 macOS にするのは実態とずれる。
→ **Windows機を触るときに4枚まとめて撮り直す**のが筋、という判断。

**準備は macOS 側で実測済み**。次にやるとき、ここは疑わなくてよい:

- **デモ画像26枚は `docs/images/SOURCES.tsv` から取り直せる**。Commons の API で
  ファイル名から原寸URLを引き、規約どおり User-Agent を名乗って1.5秒間隔で落とす。
  2026-08-15に**26枚すべて取得できることを確認**（計285MB）。ファイル名は
  `File:` を外したものをそのまま使う
- **識別子を `-demo` に変える手は macOS でも効く**。`tauri.conf.json` の
  `identifier` を `dev.harusame.pictkura-demo` にしてビルドすると、設定とDBは
  `~/Library/Application Support/dev.harusame.pictkura-demo/` に作られ、
  本物に触らない（Windowsの `%APPDATA%` と同じ理屈）
- **設定ファイルを先に置けば、フォルダ登録のGUI操作が要らない**。
  そのフォルダに `pictkura.toml` を置いて `[library] roots = ["<デモ画像の場所>"]`
  と書いておくと、起動した時点で走査が始まる。2026-08-15に26件の索引と
  サムネイル生成まで通ることを確認した
- **ビルドは速い**（識別子だけの変更なら release で42秒）。`ui` の `dist` が
  出来ていれば `npm --prefix ui run build` も一瞬

**撮り終えたら識別子を戻すこと**。戻し忘れたまま配ると、利用者の設定フォルダが
`-demo` 側になり、次の版で戻したときに設定が消えたように見える。

## macOS で作業するとき（2026-08-14）

開発機をmacOS（Apple Silicon）へ移したときに分かったこと。

- **rav1d 1.1.0 は crates.io 版がARM64で壊れている**。`Cargo.toml` の
  `exclude` に `*.h` が入っているためパッケージに `src/arm/asm-offsets.h` が含まれず、
  ARM64のasm（`refmvs.S` / `filmgrain.S` / `filmgrain16.S`）が `#include` に失敗する。
  回避は `[patch.crates-io]` で git のタグ `v1.1.0` に差し替えること:

  ```toml
  [patch.crates-io]
  rav1d = { git = "https://github.com/memorysafety/rav1d", tag = "v1.1.0" }
  ```

  **2026-08-14に判断を覆してコミットした**（同日、macOSを配ると決めたため。
  それまでは「配るものの中身に効く差し替えなので、実際に配ると決めるまで動かさない」
  として未コミットで持っていた）。`[patch.crates-io]` は**ターゲット別に書けない**ので、
  これは**Windowsの配布物にも効く**——供給元が crates.io から git に変わる。
  版は同じタグなので中身は上流そのもの。取らなかった道は
  「aarch64だけ `asm` feature を落とす」（AVIFデコードが6倍遅くなり「速いことが仕様」に
  正面から反する）と「上流に `exclude` の修正を出す」（マージと新版リリースの時期が
  読めず、リリースが無期限に止まる）。
  **上流の `exclude` が直った版が出たら、`Cargo.toml` の patch を消してcrates.ioへ戻すこと**
- **テストはmacOSでも全部通る**（移した当日は264件、パッケージ除外の回を入れて
  **2026-08-14時点で272件**。Windowsとの差は `cfg(windows)` のぶん。
  件数は増えるので、**この数字ではなく緑かどうかを見ること**）。
  移した直後は10件落ちたが、**すべてテスト側がWindowsの環境を前提にしていたのが原因**で、
  製品コードの欠陥ではなかった。DBもパス正規化も、既に**文字として**両方の区切りを扱っている
  （`pk_name` / `pk_folder` / `parent_dir_expr`）。直した内容は
  「2ゲートレビューで直したところ（2026-08-14）」を見ること
- **`src-tauri/gen/schemas/` は生成物**。ビルドのたびにACLから作り直され、
  **作られる顔ぶれがOSで変わる**（macOSでは `macOS-schema.json` が増える）。
  中身は `desktop-schema.json` / `windows-schema.json` とバイト単位で同一なので、
  追跡から外した（Tauri v2の公式テンプレートも同じ扱いをしている）

### アプリはmacOSで起動する（2026-08-14に実測）

`target/debug/pictkura`（Mach-O arm64）をそのまま実行するとウィンドウが出て、
走査もサムネイル生成も動いた。設定とDBは `~/Library/Application Support/dev.harusame.pictkura/`。
**「動かない」ではなく「一部の機能が無い」**が正しい（動画のサムネイル・HEICの画素・
`shell::metadata` はWindows専用のまま）。

ただしそのとき**写真.appのパッケージを索引する事故**を踏んだ。
既定のルートが `picture_dir()`＝`~/Pictures` になり、その配下の
`写真ライブラリ.photoslibrary` の中身を **14,938件・サムネイル5,399枚・84MB**
（1分ちょっと）ぶん索引した。`.photoslibrary` は見た目こそフォルダだが
写真.appが管理するパッケージで、中身はUUID名の内部ファイル。
既定の除外（`.*` / `Thumbs.db` / `$RECYCLE.BIN`）は**ドットで始まる名前**が対象なので当たらない。

→ 既定の除外に `*.photoslibrary` を足した。**これはmacOS対応ではなくv0.1の品質**——
Macから移った人が外付けHDDやNASへコピーしていれば、Windowsでもただのフォルダとして
同じ事故が起きる。

**この手の既定値は「タグを打つ前」にしか安く直せない。** 初回起動時に
`config.save()` が設定を**全量**書き出すので（`lib.rs` の `first_launch`）、
一度でも起動した環境のTOMLには当時の既定が焼き付く。あとから既定を変えても届かない。

**引き上げの仕組みは置かないと決めた。** `upgrade_extensions` と同じ
LEGACY_DEFAULTS方式を一度は入れたが、新しい既定が旧既定＋1件なので、
**利用者が `*.photoslibrary` を消した設定は旧既定と同じ形になり、起動のたびに
書き戻される**（ゲート2の指摘）。除外を編集するUIは無くTOMLの手編集が唯一の
逃げ道なので、そこを塞ぐと直す手段が無くなる。v0.1をまだ配っていない以上、
引き上げが要る既存の環境は開発機だけ（TOMLを消せば済む）。
**配ったあとに既定を変える必要が出たら、中身から推測せず設定にスキーマ版を持たせること。**

除外パターンは `scan_fingerprint` に入っているので、変えると次回はフルスキャンへ
落ち、`apply_scan` が索引済みのレコードを掃き出す。ただし**掃き出されるのは
DBの行だけで、サムネイルのファイルは残る**（下記）。

**写真.appの系譜はまとめて入れた**（`*.photolibrary` / `*.migratedphotolibrary`
＝iPhoto、`*.aplibrary` ＝Aperture。2026-08-14のユーザー判断）。
実物を踏んだのは写真.appだけだが、どれも**同じ `~/Pictures` に住む**Appleの
パッケージで事故の形が完全に同じであること、そして引き上げ機構を外した以上
**既定に入れ損ねると走査途中の除外が既存の利用者へ永久に届かない**ことによる
（ゲート2の指摘）。

Lightroomの `*.lrdata` は入れていない。あれは `~/Pictures` の既定ルートに
必ず居るわけではなく、系譜も違う。

**ルートに指定する経路も塞いだ。** 走査は `depth 0` を除外判定しない
（scanner.rs の `filter_entry`）ので、パッケージ（やその中）をルートに登録すると
索引される一方、監視とUSNは `is_excluded_path` が**全構成要素**を見るため
更新イベントを全部落とす——**索引されるのに永久に古いまま**という壊れた状態になる。
そこで `add_library_root` と取り込みの3経路（`import_from` /
`list_source_dir` / `list_source_tree`）で断る。判定は
`import::is_managed_package_path` で、**葉の名前だけでなく全構成要素**を見る
——取り込み元も追加するルートもネイティブのフォルダ選択ダイアログで選ぶので、
`E:\Photos Library.photoslibrary\resources\derivatives` のように**中を名指しできる**。

**走査側でも弾く**（2026-08-14のユーザー判断）。UIの経路を全部塞いでも、
`pictkura.toml` に直接書かれたルートは残る（ゲート1・ゲート2が独立に指摘）。
そこで `depth 0` を除外判定しない例外を、**固定一覧（`MANAGED_PACKAGE_PATTERNS`）
にだけは適用しない**ことにした。`.*` で始まるフォルダをルートに指定したい人の
逃げ道は残る。掃き出しは効く（`had_error` を立てないので `ok_roots` に入り、
既に索引されていた行は消える）。

**効き方は2種類あり、混同しないこと**（ゲート2がコメントとの食い違いを指摘）:

- **入口では固定**（設定に関係なく断る）: ルート登録・コピー先の設定・
  取り込み元の指定と一覧。ルートに名指しされたときだけは例外を打ち消す必要がある
- **走査の途中では設定に従う**: 既定の `exclude_patterns` に同じものが入っており、
  消せば配下のパッケージは索引される。ここを固定にすると**TOMLの手編集という
  唯一の逃げ道が塞がる**（引き上げ機構を外したのと同じ理由）

判定（`is_managed_package_path`）を当てる場所は**設定されたルートを受け取る所だけ**。
具体的には `scan_roots_pruned`（起動時・手動同期の本線）と、アプリ層の門
（`add_library_root` / `set_import_destination` / 取り込みの3経路）。

当てては**いけない**所を3回間違えたので、理由ごと残す:

- `walk_pruned` の中 … 再帰のたびに全構成要素を舐める。mtime枝刈りの高速路
  （`metadata` 1回）に釣り合わない
- USNの dirty dir と監視のイベント … ルートではなく走査の途中。当てると
  opt-out した人の差分更新だけが黙って捨てられ、フルスキャンと食い違う。
  ただし**ルート自身がパッケージなら配下は落とす**（そうしないと差分が入れて
  フルスキャンが消す、の往復になる）。**両方に同じ絞り込みが要る**
  ——USN側だけ直して監視側を忘れ、次の周で指摘された
- `scan_roots` の `depth 0` … ここに渡るのは設定されたルートではなく、
  監視が拾った「移動されてきたフォルダ」と取り込み元（手前で判定済み）。
  当てると監視だけ索引せず次のフルスキャンは索引する、という食い違いになる

**除外パターンは走査の前に一度だけ小文字化する**（`lower_patterns`）。
`matches_pattern` は呼ぶたびに両側を `to_lowercase()` するので、既定が7つある
いまは1エントリあたり14回の確保になっていた。内側のループでは
`matches_pattern_lower` を使う。

なお `depth 0` の例外そのものは残っている。`.*` のような**利用者のパターン**を
ルートに指定した場合は、いまも索引されるのに監視が落とすという食い違いがある。
直すなら監視・USN側の判定を**ルートより下の構成要素だけ**に限ればよい
（`root_specs` / `rebase_to_root_spelling` が既にある）。

**直さないと決めた2件**（ゲート2の指摘。2026-08-14）:

- **パッケージの中の `originals` を取り出せない**。`originals/` に入っているのは
  派生画像ではなく**本人の元ファイル**なので、「Macから移った人が外付けHDDへ
  ライブラリを丸ごとコピーしていて、それが唯一の控え」という場合に取り出す道が
  無くなる。取り込み経路は設定を見ない作りなのでTOMLの手編集でも回避できない。
  → **フォルダ名から `.photoslibrary` を外せば普通のフォルダとして扱える**ので、
  逃げ道が無いわけではない。エラー文にその旨を書いた。
  例外（`originals`/`Masters` だけ通す）を作るかは、要望が出てから決める
- **取り込み元の配下にパッケージがあると黙って飛ばす**（「N件コピー」としか出ない）。
  これは `.*` のフォルダを飛ばすときと同じ挙動で、除外の通例どおり。
  取り込み元**そのもの**がパッケージのときだけ声を上げるのは、結果が0件になって
  「USBに写真が無い」と区別が付かなくなるため

### 直っていない穴: サムネイルの孤児（2026-08-14に判明）

`apply_scan` が `media` の行を消しても、**サムネイルの実ファイルは残る**。
`thumb_cache_usage` も `evict_final_thumbs` も `media` の行を起点にしているので、
行が消えた時点でそのファイルは**容量上限の勘定からも消え、LRUの候補にもならない**
（`db.rs` の該当関数と `lib.rs` のjanitor）。孤児の掃除はパスの綴り統一の移行
（段階G-3）にしか無い。

これは今回の事故に限らない。**利用者が写真を消した・ライブラリのルートを外した
ときも同じ**で、消したぶんのサムネイルがディスクに残り続け、上限で守れなくなる。
今回のパッケージ索引で作られた5,399枚・84MBも、除外を足しただけでは消えない
（開発機では設定ごと消したので実害なし）。v0.1には入れないが、**孤児の掃除は
入れるべき**——`thumbs` 配下を走査して `media` に無いものを落とす一掃を、
起動時か janitor に足すのが素直。

## macOSも配る（2026-08-14のユーザー判断）

**Windows版とmacOS版の両方をGitHub Actionsで作る**と決めた。前提だった rav1d の
判断（上記）を覆してコミットしたのが起点。

| 決めたこと | 中身 |
|---|---|
| 対象 | **Apple Silicon（arm64）のみ**。Intel（x86_64）版とUniversalは作らない |
| 配布形式 | **`.app` を収めたZIP**。DMGは後日の判断 |
| 署名 | **ad-hoc署名のみ**。Developer ID の署名も公証もしない |
| 最低OS版 | **11.0**（Apple Siliconの実機の下限。Tauri既定の10.13は嘘になる） |

**DMGではなくZIPにしたが、Gatekeeperの観点で両者に差は無い。** ブラウザで
ダウンロードすると `com.apple.quarantine` が付き、**展開した `.app` にも伝播する**ので、
未署名ならどちらの形式でも初回は弾かれる。ZIPを選んだのはCIが単純だからで、
「ZIPなら回避できる」からではない。回避手順はZIPに同梱する
「はじめにお読みください.txt」とREADME（英日とも）に書いた。
**当初は「右クリック→開く」を主に書いたが、これは2026-08-15に誤りと判明して
差し替えた**——下の「実測: 初回起動の案内が間違っていた」を見ること。

### 実測で分かったこと（2026-08-14・開発機のApple Silicon）

- **release ビルドは通る**。`cargo build --release` が1分27秒、`cargo tauri build` の
  `.app` バンドルまで通る。ZIPは7.3MB（Windows側のポータブルZIPは8.1MB）
- **NASMは要らない**。あれはx86のアセンブラで、ARM64では rav1d も libjpeg-turbo も
  `.S` を clang に通す。**Windows側とは事情が違う**のでCIでも入れていない
- **`.icns` はリポジトリに要らない**。`src-tauri/icons/` にあるのは `.ico` と 256×256 の
  `.png` だけだが、tauri-bundler が png から `pictkura.icns` を作る。
  ただし**元が256pxなので、Finderの大アイコン表示では甘い**。1024pxの原本を用意すれば直る
- **資源は `Contents/Resources/_up_/` に入る**。`tauri.conf.json` の `resources` を
  `../` 付きで書いているため（`lib.rs` の `about_info` のコメントにある `_up_` の罠と同じ）。
  `.app` の中で完結するので、ZIPの側に取説を置く必要は無い

### 踏んだ罠: バンドルを署名しないと「未署名より悪い」状態になる

最初は署名の設定を入れずに作った。すると `codesign -v` がこう言う:

```
code has no resources but signature indicates they must be present
```

**arm64のリンカは実行ファイルに勝手にad-hoc署名を付ける**（`flags=0x20002(adhoc,
linker-signed)`、`Identifier=pictkura-0e6f515c462272a7` という自動生成の識別子）。
一方で `.app` バンドルの側は署名されないので `Contents/_CodeSignature` が無く、
**署名が「壊れている」状態**になる。単なる未署名より悪く、quarantineを外しても
弾かれる経路が残る。

`tauri.macos.conf.json` に `"signingIdentity": "-"` を置いて `cargo tauri build` に
バンドルごと署名させると直る（`Identifier=dev.harusame.pictkura`、
`flags=0x10002(adhoc,runtime)`、`_CodeSignature/CodeResources` が出来る）。
**CIの点検にも `codesign -v` を入れた**——ZIPから出したものに対して掛けるので、
`ditto` が署名を保つことまで込みで見る。

なお `.app` をZIPに詰めるのは **`ditto` を使うこと**。`.app` はシンボリックリンクと
実行権限を含むので `zip -r` では壊れ、しかも**ZIPは正常に見える**（起動しない
`.app` が配られる）。

### 見つかった穴: 監視が返す綴り（2026-08-14・macOSをCIに載せて判明）

**macOSをCIに載せた初回で、`watch::tests::ファイル追加イベントがバッチで届く` が落ちた**
（開発機では通っていた）。CIが返したのは
`/private/var/folders/.../T/.tmpQAdK8f`、テストが比べていたのは `dir.path()` ＝
`/var/folders/.../T/.tmpQAdK8f`。**macOSの `/var` は `/private/var` への
シンボリックリンクで、FSEventsは解決後の綴りを返す**。

テスト側は両側を `canonicalize` して直した。開発機で通っていたのは、FSEventsが
**ファイル単体のイベント**を返したから（`ends_with("new.jpg")` の枝で通っていた）。
CIでは**親フォルダ**が返り、壊れていた方の枝に落ちた。どちらが来るかはFSEventsの
束ね方次第なので、**この差はいつ開発機側で出てもおかしくなかった**。

**製品側にも同じ根の穴が残っている（直していない）。**
`rebase_to_root_spelling`（綴りを設定ルートへ揃える処理）は
**`try_usn_sync` にしか掛かっておらず、監視経路の `handle_fs_events` は素通し**。
したがってシンボリックリンク配下のルートを登録すると、監視は解決後の綴りで、
フルスキャン（`scan_roots_pruned` は設定の綴りから歩く）は設定の綴りで入れ、
**同じ写真が2件登録される**——段階G-3と同じ系統。

**同じ食い違いがパッケージ除外の門も外す**（ゲート2が指摘。こちらは記録し漏れていた）。
`handle_fs_events` の `package_roots.iter().any(|root| p.starts_with(root))` は
設定の綴りと比べるので、解決後の綴りが来ると**一致しない**。つまり監視だけが
パッケージの中身を索引し、次のフルスキャンがそれを消す——`lib.rs` の
そのすぐ上のコメントが「やってはいけない」と書いている往復そのものが起きる。

直さなかったのは、これが**パスの綴りの手術**で、macOS配布のPRに混ぜる範囲ではないから
（混ぜると2ゲートがどちらの変更を見ているのか分からなくなる）。現実の露出は小さい:
ルートはネイティブのフォルダ選択ダイアログで選ぶので、`~/Pictures` や `/Volumes/...`
なら解決前後で綴りが変わらない。`/tmp` や `/var`、あるいは自分で張ったシンボリック
リンク経由でライブラリを登録した人が対象。

**直すなら**、`handle_fs_events` にも `root_specs` / `rebase_to_root_spelling` を通すのが
素直（USN側と対にする。「USN側だけ直して監視側を忘れ、次の周で指摘された」のと同じ形）。
ただし `rebase_to_root_spelling` は `/` を `\` へ寄せる**Windows前提の正規化**なので、
macOSで使うには先にそこを直す必要がある。

### 実測: 初回起動の案内が間違っていた（2026-08-15・macOS 26.3.1）

**v0.1.0 の配布物そのもの**（Releases から `pictkura_0.1.0_arm64.zip` を落とし、
ブラウザ相当の `com.apple.quarantine` を手で付けて再現）で測った結果、
**README と ZIP 同梱テキストの初回起動の案内が二重に間違っている**ことが分かった。

| 測ったこと | 結果 |
|---|---|
| quarantine の伝播 | ZIP → `.app` → `Contents/MacOS/pictkura` **すべてに伝播**（既存の記述どおり） |
| `ditto` が署名を保つか | **保つ**。`codesign -v` は通り `Identifier=dev.harusame.pictkura` / `flags=0x10002(adhoc,runtime)` |
| `spctl -a -t exec` | **`rejected`**。しかも **`xattr -dr` で quarantine を外したあとも `rejected` のまま** |
| ダブルクリック | **弾かれる**。文言は下記 |
| **右クリック→「開く」** | **効かない。同じダイアログが出るだけ**（2026-08-15にユーザーが実機で確認） |

**出るダイアログは「壊れているため開けません」ではない**:

```
"pictkura" は開いていません
Apple は、"pictkura" に Mac に損害を与えたり、プライバシーを侵害する
可能性のあるマルウェアが含まれていないことを検証できませんでした。
　　［ゴミ箱に入れる］　［完了］
```

つまり間違いは2つ。**(1) 文言が違う**——英日とも「壊れているため開けません」
「is damaged and can't be opened」と書いているが、実際は**公証の検証に失敗した**
という別の定型文（macOS 15 Sequoia 以降のもの）。**(2) ボタンに「開く」が無い**
——［ゴミ箱に入れる］と［完了］だけで、ダイアログ上に先へ進む道が無い。
**macOS 15 で control＋クリックのバイパスが廃止された**ため、
「右クリック→開く」を勧めている案内は**今日の受け取り手のほぼ全員に効かない**。

`spctl` の行が効く: 公証が無い以上 Gatekeeper の評価は**常に不合格**で、
実際に弾かれるかは quarantine が付いているかだけで決まる。だから
**`xattr -dr` は今も確実に効く**——直すべきは「方法1」の側。

**正しい代替経路（同日に実機で確認）**: **システム設定 →「プライバシーとセキュリティ」→
下までスクロール → `pictkura` がブロックされた旨の行の「このまま開く」→ パスワード**。
これで**`.app` が実際に起動した**。ただし**先にダブルクリックして一度弾かれておく**必要が
ある（その行は弾かれた記録があって初めて出る）。README英日とZIP同梱テキストは
この手順に書き換えた（`xattr -dr` はターミナル派向けの第2案に落とした）。

**承認は `spctl` には現れない。** 承認後も `spctl -a -t exec` は `rejected` のままで、
変わるのは**quarantine のフラグ**（`0083` → `00c3`、`0x40` が承認ビット）と
`com.apple.provenance` が付くこと。CIや手元で「開けるようになったか」を
`spctl` で測ろうとしても分からない——見るならフラグの方。

**App Translocation を踏んだ**（副産物の発見）。展開した場所から直接起動したため、
アプリは `/private/var/folders/.../AppTranslocation/<UUID>/d/pictkura.app` という
読み取り専用の写しから動いていた。quarantine 付きのアプリを「移動せずに」起動すると
macOSがこうする。同梱テキストが元から書いていた「好きな場所へ移してから実行」は
**作法ではなくこれを避ける手順**だったので、README にも理由を添えて残した。

### まだ確かめていないこと

- **macOS 11〜14での動作**。`minimumSystemVersion` に11.0と書いたが、
  実際に確かめたのは開発機の版だけ。**書いた値は「arm64の下限」であって実測ではない**。
  初回起動の手順は**macOS 15 を境に変わる**ので、READMEと同梱テキストは版で
  分けて書いた（15以降＝システム設定の「このまま開く」／11〜14＝右クリック→開く）。
  **11〜14 の側は実機で確かめていない**——手元にその版の機械が無い

### 2ゲートレビューで直したところ（2026-08-15・初回起動の案内の回）

**ゲート1（codex）は1件（P2）**。**版で手順を分けていなかった**という指摘。
`minimumSystemVersion` は 11.0 なのに、書き換えた手順は macOS 15 以降のものしか
無く、macOS 11〜12 には「システム設定 → プライバシーとセキュリティ」という画面が
そもそも存在しない（当時は「システム環境設定 → セキュリティとプライバシー」）。
対象に入れている利用者が手順を踏めない。

**この穴は自分でも上の「まだ確かめていないこと」に書いていたのに、READMEへ
反映していなかった。** 記録に残すことと直すことは別、という分かりやすい例。

→ READMEと同梱テキストを**版で分けた**。macOS 11〜14 の側は
**「右クリック→開く」だけを書き、画面の名前は出さない**——手元にその版の機械が
無く、`システム環境設定 →「セキュリティとプライバシー」→「一般」` と書いても
それは実測ではないため。**確かめていない画面名を書かない**のが、今回直している
不具合そのものを繰り返さない唯一の方法。

### macOS版に無い機能（Windows版との差）

`README` の注意事項に既にあるものと同じ。ZIPの同梱テキストにも書いた。

- 動画のサムネイル（`shell::thumbnail` がWindows専用。QuickLookの枝は未実装）
- HEIC/HEIFの画素（macOSは ImageIO を使えば行けるが未実装）
- クラウドにしか実体が無いファイルの素性（`shell::metadata` がWindows専用）

### 2ゲートレビューで直したところ（2026-08-14・macOS配布の回）

**ゲート1（codex）は1件**。`tools/release-macos.sh` にアーキテクチャの門が無く、
ワークフローにしか無い、という指摘。**呼ばれる側に門を置いた**（手元から呼んでも効く）。

**ゲート2（Opus）は6件**。効いた順に:

- **門が「機械」を見ていて「作った物」を見ていなかった**（一番効いた）。
  `uname -m` はApple Silicon機なら `arm64` を返すが、**rustup の既定ホストが
  `x86_64-apple-darwin` だと出てくるバイナリは x86_64**（Rosetta下でrustupを
  入れると起きる）。それを `..._arm64.zip` という名前で配ることになり、
  止めたかったことがそのまま起きる。→ ビルド後の `lipo -archs` を本当の門にし、
  **ZIPの名前もその値から付ける**（素性と名前の出どころを1つにした）。
  `uname -m` の方は「10分のビルド前に明らかなIntel機を弾く」役として残した
- **前の版のZIPが隣に残りうる**。`target/` はCIのキャッシュから復元されるので、
  版を上げた最初の実行で `ls .../*.zip` が2行返る。員数確認も4つではなく5つに
  なって落ちる。→ スクリプトは置き場ごと作り直し、ワークフロー側は独立に1つを数える
- **`codesign -dvv | grep -q` が正しい署名で気まぐれに落ちる**。`grep -q` は
  一致した時点で終わるので、まだ書いている `codesign` がSIGPIPEで死に、
  `pipefail` が終了141を拾う。→ 先に変数へ受けてから見る
- **タグと `tauri.conf.json` の版が食い違っても素通りする**。配布物の名前は
  すべて `tauri.conf.json` から付くので、上げ忘れたまま `v0.2.0` を押すと
  「v0.2.0」のReleaseに `pictkura_0.1.0_*` が4つ並び、**員数は4つなので通る**。
  → `publish` の先頭で照合する
- **マトリクス化で報告名が変わる**（`check` → `check (windows-latest)` 他）。
  ブランチ保護が `check` を必須にしていると永久に待つ、という指摘。
  **このリポジトリは保護もルールセットも掛けていないので実害なし**（実地で確認）。
  ただし「両方を必須にしている」と読める私のコメントが誤解の元だったので、
  掛けるときの注意に書き換えた
- **監視の綴り問題**（上記）。plan.md が先送りを宣言していることを確認したうえで、
  **私が記録し漏れていた二次被害**——パッケージ除外の門も同じ理由で外れる——を指摘した。
  記録に足した。直すのは別PR

## 2ゲートレビューで直したところ（2026-08-13 CI の回 / 2026-08-14 macOS の回）

**CIの回**。この機械にcodexが無かったのでOpusとFableの2者に見せた。

- **テストの検出力が落ちていた**（両者が独立に指摘・一番効いた）。
  `usn`: 8.3短縮名を解決するのに `canonicalize` を**両側**に掛けたため、
  「`resolve_frn` は長い綴りを返す」という契約を張っていた唯一の網が消えていた。
  退行しても緑のまま通り、その先で `rebase_to_root_spelling` の前方一致が外れて
  **差分同期が黙って取りこぼす**（段階G-3と同じ系統）。→ 比較を非対称にした
  （期待値だけ解決、製品の出力は小文字化のみ）。
  `namedate`: `assert_ne!` → `assert_eq!` の修正は方向は正しかったが、
  **UTCの機械では検出力がゼロ**だった。→ 判定関数 `is_utc_naming` を直接assertする形にした
- **タグを push すると `ci.yml` が走らない**穴（Opusが単独で発見）。→ 上の「落とし穴」に記載
- 配布物の点検、`workflow_dispatch` でタグrefを選んだときに公開してしまう件、
  `permissions` / `concurrency` / `persist-credentials` / NASMの版固定

**macOSの回**（PR #2 / #3）。両ゲートとも指摘なし。ゲート2は周辺まで洗って、
同じ系統の穴が他に無いこと（`db.rs:2173` / `:2245` は既にgate済み）、
`search_names` の変更がWindowsで挙動不変であること、`unwrap_or("")` が到達不能であること、
生成スキーマが本当に再生成されること（`tauri-build` の `create_dir_all`）を確認した。

## 大きめの残件

### `.m2ts` のアプリ内再生（やるならB案：自前remux）

**2026-08-13にユーザー判断**: v0.1を先にやる。やるなら**自前のMPEG-TS→fMP4 remux**で、
**ffmpeg同梱は取らない**（「最小機能」の看板に対して重い）。

実測で分かっていること:

- remuxは**7MBで0.14秒**。復号しないので実質I/O律速
- **AC-3は障害にならない**。remuxしたH.264+AC-3のMP4をWebView2は映像も音も再生した
  （AVCHDの音声はAC-3なので、ここが通らないと作る意味が無かった）
- **この機械のライブラリに `.m2ts` / `.avi` の実物は1つも無い**（検証は合成ファイルのみ）

設計案: 開いた時に1回だけ走査して sample index を作り（画素に触れない）、
Range要求ごとに `moof`+`mdat` を組んで返す。常駐はindexだけで1時間の動画でも約1MB
——2GBでも常駐4MiBという今の性質（`protocol::plan_range`）を壊さない。
規模は TS demux（188/192バイト・PAT/PMT/PES）＋ AnnexB→AVCC（SPS/PPS→`avcC`）＋
音声のサンプル記述 ＋ fMP4 muxer ＋ シークで、**1,000〜1,500行**。
**`.avi` は救えない**（コンテナではなくコーデックが無い）。

### macOS / Linux の対応

**macOSは2026-08-14から配っている**（「macOSも配る」の節）。配ってはいるが、
下の3つはWindows専用のまま——「動かない」ではなく「一部の機能が無い」状態。
**開発機がApple Siliconになったので、いまはここを検証できる**。

- **動画のサムネイル**: `shell::thumbnail` が唯一の入口。ここに QuickLook
  （`QLThumbnailGenerator`）/ ディストリのサムネイラの枝を足す。
  macOS版を配った以上、**ここが一番効く残件**（動画が枠だけ並ぶ）
- **HEIC の画素**: macOSは ImageIO、Linuxは非対応のまま。
  iPhoneの既定形式なので、macOS利用者ほど当たりやすい
- **`shell::metadata`（段階H）もWindows専用**。macOS/Linuxではクラウドのみの
  ファイルの素性が空のままになる

**監視が返す綴り**（`handle_fs_events` が `rebase_to_root_spelling` を通らない件）も
ここに属する。詳しくは「見つかった穴: 監視が返す綴り」の節。

macOS版の配布そのものの残件は次の2つ。

- **Developer ID 署名と公証**。いまは ad-hoc署名だけなので、受け取った人は初回に
  **一度弾かれてから、システム設定→プライバシーとセキュリティ→「このまま開く」**を
  踏むことになる（2026-08-15に実測。「右クリック→開く」は macOS 15 以降きかない）。
  署名して公証すれば、この5手順がまるごと要らなくなる——**受け取り手にとっては
  一番大きな改善**。Apple Developer Program（年 $99）への加入が前提
- **DMG**。「後日の判断」として保留。Gatekeeperの観点ではZIPと差は無いので、
  見栄え以外の理由は無い

### 使い勝手の穴

- 検索結果の並び替えが無い（撮影日の降順で固定）
- 索引の手動再構築UIが無い（`pictkura.db` を消して作り直すしかない）
- 取り込み元ツリーのキーボード操作が無い
- 取り込みの中断ができない
- RTL（アラビア語等）は未対応。CSSの論理プロパティ化が前提
- E2E が無い（CIは2026-08-13〜14に用意した。「いまどこ」の節）

## 運用の約束（AIとの進め方）

- **2ゲートレビュー**をマイルストーンごとに必ず実施する。
  ゲート1 = `codex review --base <ref>` / ゲート2 = Claude の `/code-review high <範囲>`。
  **`codex review --commit <sha>` は未コミットの作業ツリーを巻き戻す**ので、実行前に必ずコミットする
- **レビューに掛ける前に、無関係な未コミット変更は `git stash` で外す**。
  レビュアは作業ツリーを見るので、混ざっていると**そこを差分だと誤認する**
  （2026-08-13、rav1dの実験を「配布物に入る」と誤って指摘された）
- **codex CLI 0.147.0 では `--base` / `--commit` / `--uncommitted` と自作プロンプトが排他**
  （`error: the argument '--base <BRANCH>' cannot be used with '[PROMPT]'`）。
  範囲を指定すると既定のプロンプトになり、リポジトリの流儀を渡せない。
  範囲の正しさを優先すること。**codex自身は `cargo test` を走らせようとする**ので、
  macOSでは rav1d のビルドで落ちる——テストの裏取りは自分でやる
- **測ってから決める**。このファイルの実測値はすべて手元で取ったもの。
  推測で設計を変えない
- **用語**: メディアの入れ物は「**コンテナ**」（「容器」は使わない）
- READMEは**英語が標準（`README.md`）／日本語は `README.ja.md`**。相互リンクを保つ

## 踏んだ落とし穴（機械が変わっても効くもの）

- **アプリの起動は切り離す**: `nohup ./target/release/pictkura.exe > /dev/null 2>&1 & disown`。
  `cmd && cmd &` だとシェル終了で落ちる。**起動したまま `cargo build` すると
  実行ファイルを掴んだままでリンクに失敗する**（os error 32）ので、先に止める
- **debugビルドはサムネイル生成が10倍以上遅い**。速度を見るときは必ず `--release`
- **GitHubの二重アカウント**: pushが「Repository not found」になったら、
  `gh auth status` でアクティブアカウントがbot側に戻っていないか見る
- **OneDriveのプレースホルダ**: ライブラリの大半はクラウドのみ。
  調査で一括read すると本物のダウンロードが走る。**属性を見るだけなら安全**
- **DBへの書き込みトランザクションは必ずIMMEDIATE**（`Db::write_tx`）。
  DEFERREDだと `SQLITE_BUSY_SNAPSHOT` が `busy_timeout` を無視して即返る
- **DBの列を埋める変更を入れたら、その列を条件にしている既存のスイープを必ず洗う**。
  段階Hで `width` を埋めた結果、カメラ補完（`camera_id IS NULL AND width IS NOT NULL`）に
  クラウドのみのファイル3,165件が並び、**次回起動でライブラリ全体がダウンロードされる**
  ところだった（Codex P1で発見）
- **一度きりの移行に「済んだ印」を持たせない**。印を先に書くと、投げた仕事が終わる前に
  アプリが落ちたとき二度と拾い直せない。**条件から自然に外れる形**に設計する
- **タグの push では `ci.yml` が1行も走らない**（`on.push.branches` フィルタは
  タグのrefにマッチしない）。CIが赤のコミットにタグを打つだけで未検査の配布物が
  Releasesに載る経路になるので、`release.yml` 側でも fmt とテストを通している
- **`if-no-files-found: error` は検索結果が「全部空」のときしか発火しない**。
  MSIだけ出てZIPが無い・言語が1つ欠けた、を素通しする。**数えて確かめる**
- **テストのパスをOSに解釈させない**。Windowsの綴り（`D:\...`）を
  `Path::file_name` に渡すと、macOS/Linuxでは区切りと見なされず**パス全体が
  ファイル名として返る**。製品側（`pk_name` / `parent_dir_expr`）は文字で切っているので、
  テストもそちらに揃える
