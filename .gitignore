# Rust
/target/
**/*.rs.bk

# Node (フロントエンド導入後に使用)
node_modules/
dist/

# ローカル設定・ログ
*.log
.DS_Store
Thumbs.db

# TypeScriptの差分ビルド情報（生成物）
*.tsbuildinfo

# TauriがビルドのたびにACLから作り直すスキーマ（生成物）。
# 作られる顔ぶれはOSで変わる（macOSでは macOS-schema.json が増える）ので、
# 追跡すると開発機を替えるたびに差分が出る
src-tauri/gen/schemas/

# 開発日誌（非公開リポ Harusame64/pictkura-dev）を入れ子でcloneする場所。
# コードのコメントにある「plan.md 第3部」等の参照先はそこにある
/dev/
